# Luna Multiplayer

## Project Overview

**Luna Multiplayer (LMP)** is a multiplayer mod for Kerbal Space Program 1. Players share a persistent universe, sync vessels in flight, coordinate docking, and share career-mode resources (funds, science, contracts) across one server. This repository is a personal fork of [LunaMultiplayer/LunaMultiplayer](https://github.com/LunaMultiplayer/LunaMultiplayer) (upstream). Our remote `origin` is [Majestic95/LunaMultiplayer](https://github.com/Majestic95/LunaMultiplayer).

- **Goals on this fork:** stability fixes, shared-physics improvements, eventual per-agency career progression (Stage 5).
- **Coordination:** upstream is actively maintained again by `AdmiralRadish` as of April 2026; docking, vessel coupling, scenario sync, and lock handoff are their active turf.
- **Prior art for per-agency:** [PlagueNZ/LunaMultiplayer-SplitProgression](https://github.com/PlagueNZ/LunaMultiplayer-SplitProgression) — used as **benchmark only**, not source. Our per-agency work goes ground-up on `feature/per-agency`.

---

## Tech Stack & Build Targets

| Project | Target | Runtime | Notes |
|---------|--------|---------|-------|
| `Server` | `net10.0` | .NET 10 | Dedicated headless server. Authoritative for all game state. |
| `LmpClient` | `net472` | Mono (KSP's bundled runtime) | KSP plugin DLL, loaded into KSP1. Builds locally with the .NET 4.7.2 Developer Pack + KSP DLLs in `External/KSPLibraries/` — see "Build & Run → LmpClient". |
| `LmpCommon` | `netstandard2.0` + `net10.0` + `net472` | Multi-target | Wire protocol, message types, shared utilities. |
| `LmpMasterServer` | `net10.0` | .NET 10 | Optional public master-server registry. |
| `LmpUpdater` | `net10.0` | .NET 10 | Self-update tool. |
| `Lidgren.*`, `LmpCommon`-vendored Lidgren | Multi-target | — | Low-level UDP transport (forked from `lidgren-network-gen3`). |

**SDK pin (`global.json`):** `10.0.100` with `rollForward: latestFeature`. The system `dotnet` on Windows is typically 7.x and cannot satisfy this. Use the user-installed `.NET 10 SDK` at `C:\Users\austi\.dotnet\` (installed without admin, fully reversible).

---

## Repository Structure

```
luna-multiplayer/
├── CLAUDE.md                   # This file — project bible
├── .claude/                    # Fork-local Claude Code config (hooks, review agents) — NOT for upstream
├── README.md                   # Public project README (upstream-inherited)
├── LunaMultiPlayer.sln
├── global.json                 # SDK pin (10.0.100, rollForward: latestFeature)
├── docker-compose.yml          # Dev compose (server + master server)
├── Dockerfile_Server / Dockerfile_MasterServer
├── Server/                     # Headless dedicated server (net10.0)
│   ├── Server.csproj
│   ├── MainServer.cs           # Entry point + task wiring (backups, log thread, command thread, network)
│   ├── Context/
│   │   ├── ServerContext.cs    # Server-wide singletons (clock, universe path, run flag)
│   │   └── Universe.cs         # Universe directory / persistent-state root
│   ├── System/                 # Server-side game systems
│   │   ├── BackupSystem.cs     # RunBackup() = flush; RunArchiveBackup() = snapshot. DO NOT CONFLATE.
│   │   ├── FileHandler.cs      # Canonical disk-IO wrapper. Use this, not raw File.*.
│   │   ├── Share*System.cs     # 13 Share systems broadcasting career mutations (funds, science, contracts, ...)
│   │   ├── LockSystem.cs       # Authoritative lock state (vessel control, scenario, asteroid)
│   │   ├── HandshakeSystem.cs  # Initial connection + version negotiation
│   │   ├── VesselSystem*.cs    # (in Server/Message/) Vessel proto/position/flight-state ingest
│   │   ├── ScenarioSystem.cs   # Career scenario aggregation
│   │   ├── CraftLibrarySystem.cs
│   │   ├── ScreenshotSystem.cs
│   │   └── ...
│   ├── Message/                # Lidgren message handlers (Reader/* validates, Server/* dispatches)
│   ├── Command/
│   │   ├── CommandHandler.cs
│   │   └── Command/            # 18 admin commands: backup, ban, kick, nuke, set-funds, set-science, ...
│   ├── Settings/
│   │   ├── SettingsHandler.cs
│   │   ├── Base/
│   │   ├── Structures/         # *SettingsStore static wrappers
│   │   └── Definition/         # 12 XML-serialized settings groups (Gameplay, Connection, Log, ...)
│   ├── Log/
│   │   ├── LunaLog.cs          # Server logger (extends LmpCommon BaseLogger; adds file append)
│   │   ├── LogThread.cs        # Async rollover (daily) + expire pass
│   │   └── LogExpire.cs        # Deletes logs older than LogSettings.ExpireLogs days
│   ├── Web/
│   │   ├── WebServer.cs        # uhttpsharp-based HTTP endpoint (port 8900) — admin dashboard surface
│   │   ├── Handlers/           # ServerInformationRestController + JsonGetHandler (Stage 3.7) + ExceptionHandler
│   │   └── Structures/         # ServerInformation, ForkInformation (Stage 3.7), LogSnapshot (Stage 3.7)
│   ├── Client/                 # Connected-client state and lifecycle
│   ├── Upnp/                   # Optional UPnP port mapping
│   └── Plugin/                 # Server-side mod loader (LMPModInterface)
├── LmpClient/                  # KSP plugin (net472, Mono)
│   ├── Systems/                # 50+ client systems mirroring Server/System (vessel sync, share*, locks, ...)
│   ├── Harmony/                # 30+ Harmony patches over KSP internals
│   ├── Base/                   # Base classes (System<T>, Sender<T>, StyleLibrary)
│   ├── Network/                # Lidgren client transport wrapper
│   ├── VesselUtilities/        # Docking, EVA, part-attach helpers
│   ├── ModuleStore/            # PartModule field interception + patching
│   ├── Windows/                # IMGUI windows (server list, status, chat, locks)
│   ├── MainSystem.cs           # Update loop + dispatch into systems
│   └── Utilities/Json.cs       # Vendored Newtonsoft adapter
├── LmpCommon/                  # Shared wire / utils (multi-target)
│   ├── Message/                # Message types (request/reply/data) — wire contract
│   ├── ModFile/                # Mod-control file parsing
│   ├── Time/                   # LunaNetworkTime (UTC clock used by both sides)
│   ├── BaseLogger.cs           # Shared logger base (server's LunaLog extends this)
│   └── Locks/                  # Lock primitives shared with server
├── LmpMasterServer/            # Optional public registry
├── LmpUpdater/                 # Self-updater
├── Lidgren/ Lidgren.Core/ Lidgren.Net/   # Vendored UDP transport
├── ServerTest/                 # NUnit tests for server (18 tests)
├── LmpCommonTest/              # NUnit tests for LmpCommon
├── LmpClientTest/              # MSTest on net472 — client-internal unit tests
├── Tools/
│   └── AdminGui/               # Avalonia desktop launcher + admin console (net10.0) — Tools/AdminGui/AdminGui.slnx
│       └── LunaServerGui/      # The GUI app project (`feature/admin-gui` branch, worktree F:\luna-multiplayer-gui)
├── docs/
│   └── research/
│       ├── 00-overview.md      # Inventory method
│       ├── 01-bug-inventory.md # 50 bugs catalogued by subsystem
│       └── 02-analysis/        # Phase-2 analyses (per-bug deep dives) — populated as bugs are picked up
└── Documentation/              # Upstream-inherited end-user docs
```

---

## Build & Run

### Server (the workstream this fork focuses on)

Use the user-installed .NET 10 SDK, **not** the system `dotnet`:

```bash
"/c/Users/austi/.dotnet/dotnet.exe" build Server/Server.csproj -c Release
"/c/Users/austi/.dotnet/dotnet.exe" run --project Server/Server.csproj -c Release
```

Tests:

```bash
"/c/Users/austi/.dotnet/dotnet.exe" test ServerTest/ServerTest.csproj -c Release
"/c/Users/austi/.dotnet/dotnet.exe" test LmpCommonTest/LmpCommonTest.csproj -c Release
```

### LmpClient (KSP plugin)

Targets `net472` (Mono runtime — KSP's bundled CLR). Builds locally via the same user-installed `.NET 10 SDK` once two prerequisites are in place:

1. **.NET Framework 4.7.2 Developer Pack** — `NDP472-DevPack-ENU.exe` from https://aka.ms/msbuild/developerpacks. Installs the v4.7.2 targeting pack under `C:\Program Files (x86)\Reference Assemblies\Microsoft\Framework\.NETFramework\v4.7.2\`. `LmpClient/Directory.Build.props` then wires `TargetFrameworkRootPath` so the SDK MSBuild finds it (the SDK's default resolution is unreliable on some .NET 10 SDK builds — passing this explicitly is the workaround).
2. **KSP/Unity DLLs in `External/KSPLibraries/`** — `Assembly-CSharp.dll`, `System.dll`, `System.Xml.dll`, plus 10 `UnityEngine.*.dll` modules (see `LmpClient.csproj` `<HintPath>` entries for the full list). Two sources:
   - Extract `External/KSPLibraries/KSPLibraries.7z` — but it's password-encrypted and the password lives in the upstream's AppVeyor secret (rotated 2017 commit `5f7a0026`, never made public). Upstream's current GitHub Actions workflow doesn't build LmpClient either.
   - **Copy from a local KSP install** (preferred — legally cleaner): `<KSP>/KSP_x64_Data/Managed/<dll>`. The 13 files needed total ~18 MB.

Build:
```bash
"/c/Users/austi/.dotnet/dotnet.exe" build LmpClient/LmpClient.csproj -c Release
```

Outputs `LmpClient/bin/Release/LmpClient.dll` (~3 MB) along with vendored deps. Pre-existing warnings on this project: 4 × MSB3245 (missing `System.Buffers`/`System.Memory`/`System.Numerics.Vectors`/`System.Runtime.CompilerServices.Unsafe` HintPath to a non-existent `External/Nuget/` folder — supplied by KSP's Mono at load time, not a real problem); 2 × NU1701 (CachedQuickLz package compat); 1 × CS0169 (unused field in `Windows/Mod/ModWindow.cs`). These are noise; do not "fix" them in unrelated diffs.

For end-user deployment, the DLL is copied to KSP's `GameData/LunaMultiPlayer/Plugins/` per the existing `Scripts/CopyToKSPDirectory.bat`.

### Pre-existing build warnings (do not "fix as you go")

30 warnings exist on `master`:
- `CA1416` platform warnings in `ScreenshotSystem`
- `CS0114` in `Lidgren/NetRandom.cs`
- `NU1701` in `CachedQuickLz` + `LunaConfigNode` (packaged for older TFM)

These are pre-existing noise. They belong in a deliberate cleanup pass, **not** in unrelated diffs.

---

## Coding Conventions

> **Mandatory breakage analysis** before non-trivial changes: scope, edge cases, test plan. The full discipline lives in user-memory `feedback_breakage_analysis.md`.

> **Research-first discipline.** Before recommending a scope, a fix, or a plan, read the actual files. Never substitute an Explore/Plan-agent's tidy summary for a direct read of the load-bearing files — spot-read at least 3 from the set when an agent claims a uniform pattern across many files, especially the edge cases. Cite file path + line numbers in every recommendation (e.g., `LmpClient/Systems/ShareFunds/ShareFundsEvents.cs:8-14`); if you cannot cite, you have not done the research. Pre-scope research is part of the scope — read first, propose second, implement third. When confidence is partial, say so before the user asks. Full discipline in user-memory `feedback_research_first.md`.

### C#
- **Naming:** `PascalCase` for types and public members, `camelCase` for parameters/locals, `_camelCase` for private fields. File name matches the type name (one class per file).
- **No `Console.WriteLine`.** Use `LunaLog.Normal` (general info), `Info` (white text), `Warning`, `Error`, `Fatal`, `Debug`, `NetworkDebug`, `NetworkVerboseDebug`, `ChatMessage`. The project convention is that **`LunaLog.Normal` is the user-visible info level** (existing `BackupCommand`-style usage), even though `Info` also exists.
- **No `File.*` for Universe state.** Use `FileHandler`. It canonicalizes paths, locks per file, and centralizes error handling.
- **Catch blocks must log via `LunaLog.Error` / `Fatal` or rethrow.** No silent swallowing.
- **No `record`, `init`, `Span<T>`, `Index/Range`, top-level statements, or `System.Text.Json` in `LmpClient`** — it targets `net472` / Mono.

### Enforced rules
- **File-size caps:** soft 600 / hard 900 lines for `.cs` (test files exempt). C# is more verbose than TypeScript, so caps are higher than CE's 400/500. Documented exceptions are allowed via a top-of-file comment explaining why splitting hurts more than it helps. The `.claude/hooks/check-file-size.sh` hook surfaces overshoots; treat the warning as a backlog signal.
- **Bug-review before ship.** The `.claude/hooks/require-bug-review.sh` PreToolUse hook intercepts `git commit` and blocks until a matching `.claude/review-receipts/<sha1>.txt` exists for the staged diff. Production code paths (`Server/`, `LmpClient/`, `LmpCommon/`, `Lidgren/`) cannot be committed without a review pass; docs / tests / `.claude/` are exempt. See `.claude/README.md` for the workflow and `docs/research/06-retro-review-findings.md` for the retrospective audit that drove the hook into existence.
- **Every server system needs unit tests** as it solidifies. `ServerTest/` currently covers 6 of the ~20 server systems — new behaviour adds tests rather than borrowing from the existing gap.
- **Update CLAUDE.md** when a system, command, setting, or convention changes.

---

## Git Conventions

### Branching
- `master` — tracks `Majestic95/LunaMultiplayer` (our fork's main). Tracks upstream commits we've pulled.
- `feature/per-agency` — Stage 5 per-agency career, **active** (branched from `master` 2026-05-17 at `6515e006`). All Stage 5 work lands here; nothing merges back to `master` until Stage 5 acceptance criteria (spec §11) are green and the dual-mode `PerAgencyCareer=false` regression run passes. The branch is fork-local — never push to upstream from it.
- `fix/<topic>` — short-lived branches off `master` for upstream PRs.

### Pushing
- **Push to `origin` only** (`Majestic95/LunaMultiplayer`). **Never to `upstream`** (`LunaMultiplayer/LunaMultiplayer`).
- When opening an upstream PR (from `fix/<topic>` → `upstream/master`), use explicit `git add <paths>` — **never `git add -A`** — and do not push `.claude/` or `CLAUDE.md`. They are fork-local.

### Commit messages
Conventional Commits:
```
<type>(<scope>): <short description>
```
- **Types:** `feat`, `fix`, `refactor`, `test`, `docs`, `chore`, `perf`, `style`
- **Scopes:** `server`, `client`, `common`, `network`, `vessel`, `lock`, `share`, `scenario`, `harmony`, `backup`, `command`, `settings`, `log`, `web`, `docs`, `gui`

### No AI attribution
**Silent partner rule applies to everything**: commits, PRs, issues, code comments, committed docs.
- Strip `Co-Authored-By: Claude` from commit trailers.
- Strip "Generated with Claude Code" footers from PR descriptions.
- The upstream LMP community has reverted AI-attributed contributions before (Fierce-Cat / issue #588). On *this fork* we are simply discreet; on *upstream PRs* AI attribution is a likely rejection vector.

### PR Checklist
- [ ] `dotnet build -c Release` clean (no NEW warnings)
- [ ] `ServerTest` + `LmpCommonTest` pass
- [ ] No file >900 lines (or top-of-file comment explaining the exception)
- [ ] CLAUDE.md updated if systems / commands / settings / conventions changed
- [ ] Commit subject follows Conventional Commits with an allowed scope
- [ ] No AI attribution in commit / PR / comments
- [ ] If touching docking / coupling / scenario sync / lock handoff: confirmed against `upstream/master` (AdmiralRadish-owned turf)

---

## Architecture Principles

1. **Server-authoritative.** All game state (vessel positions, locks, career resources, scenario data) is owned by the server. Clients send intents / observations; server validates and broadcasts.
2. **Shared-agency career, today.** LMP is shared-agency by design — `Funding.Instance`, `ResearchAndDevelopment.Instance`, etc. are game-wide singletons. 13 `Share*Sender` systems propagate mutations to all clients. **Per-agency** (Stage 5) lives on its own branch and is a wide-but-shallow rewrite — not architectural impossibility.
3. **Lidgren UDP, custom message protocol.** No HTTP for gameplay. Wire schemas live in `LmpCommon/Message/`. Backward compatibility is enforced via `HandshakeSystem`.
4. **System pattern.** Each subsystem is its own class in `Server/System/` (or `LmpClient/Systems/`). Senders broadcast outbound messages, readers ingest inbound. Inter-system coupling stays explicit.
5. **Harmony patches are surgical.** Each patch in `LmpClient/Harmony/` targets one KSP type. Used to intercept singleton mutations (precedent: `ContractPreLoader_Filter`).
6. **`FileHandler` is the disk gateway.** Universe state never bypasses it.
7. **Backup as a first-class operation.** `RunBackup` (flush in-memory to canonical files) and `RunArchiveBackup` (timestamped snapshot with retention + restore) are distinct.

---

## Server System Inventory

Server-side systems (selected, see `Server/System/` for the full list):

| System | File | Purpose |
|--------|------|---------|
| `BackupSystem` | `BackupSystem.cs` | Flush + timestamped archive snapshots + restore |
| `FileHandler` | `FileHandler.cs` | Canonical disk-IO wrapper |
| `HandshakeSystem` | `HandshakeSystem.cs` | Initial connect, version negotiation, identity |
| `LockSystem` | `LockSystem.cs` | Authoritative lock state |
| `Share*System` (×13) | `Share*.cs` | Career-mode broadcast: funds, science, contracts, reputation, achievements, tech, etc. |
| `ScenarioSystem` | `ScenarioSystem.cs` | Career scenario aggregation |
| `GroupSystem` | `GroupSystem.cs` | Player groups (scaffolding only — name + members, no resource fields) |
| `CraftLibrarySystem` | `CraftLibrarySystem.cs` | Craft sharing |
| `ScreenshotSystem` | `ScreenshotSystem.cs` | Screenshot ingest + distribution |
| `KerbalSystem` | `KerbalSystem.cs` | Kerbal roster sync. Phase 6.4 adds `ResolveKerbalsPathForRequester(string playerName)` internal pure helper that branches `HandleKerbalsRequest` between the legacy shared `Universe/Kerbals/` and the per-agency `Universe/Agencies/{guid:N}/Kerbals/` subdir based on `AgencySystem.PerAgencyKerbalRosterEnabled` + `AgencyByPlayerName` lookup + `FolderExists` check. Reads only — write-path routing is Phase 6.5. K1 grief-guard `CanRemoveKerbalUnderK1` stays (structurally moot under gate=on per spec §Q-K1). |
| `WarpSystem` | `WarpSystem.cs` | Time-warp coordination |
| `FlagSystem` | `FlagSystem.cs` | Flag asset distribution |
| `GcSystem` | `GcSystem.cs` | Periodic forced GC pass |
| `ModFileSystem` | `ModFileSystem.cs` | Mod-control file enforcement |
| `AgencySystem` (Stage 5 — `feature/per-agency`) | `Server/System/Agency/AgencySystem.cs` + `AgencyState.cs` + `AgencySystemSender.cs` + per-router classes | Per-agency career registry + persistence + outbound wire. Gated on `GameplaySettings.PerAgencyCareer` (default false — dual-mode invisible to shared-agency path). Lifecycle: register on handshake auth, persist via `FileHandler.WriteAtomic`, boot-load on `MainServer.LoadExistingAgencies`. Wire surface: registration messages (Handshake/CreateRequest/CreateReply/State) shipped 5.15b/c; 9 mutation+visibility messages deferred to consumer steps. **Routers**: `AgencyContractRouter` (5.17d, Q6 contract partition), `AgencyCurrencyRouter` / `AgencyTechRouter` / `AgencyResearchRouter` / `AgencyProgressRouter` (5.17e band-1, stock career), `AgencyKolonyRouter` (Phase 3 Slice B, MKS kolony), `AgencyPlanetaryRouter` (Slice C, MKS planetary), `AgencyOrbitalRouter` (Slice D, MKS orbital), `AgencyScanRouter` (Mod-compat S2 — SCANsat per-agency coverage + scanners via Path B suppression), `AgencyDMagicRouter` (Mod-compat S4, session 34 — DMagic asteroid diminishing-returns + discovered-anomaly records; same Path B suppression model), **`AgencyWolfDepotRouter` / `AgencyWolfRouteRouter` / `AgencyWolfHopperRouter` / `AgencyWolfTerminalRouter` / `AgencyWolfCrewRouter` (Phase 4 Slices B/C/D/E — MKS WOLF per-agency depots / routes / hoppers / terminals / crew-routes; CrewRouter carries the distinctive cross-agency kerbal authority gate via vessel-proxy K1-pattern scan)**. Server-side helpers (non-router): `AgencyVesselCoupleReconciler` (Mod-compat S1, session 35 — reconciles `lmpOwningAgency` on the surviving vessel after `Part.Couple`; covers stock docking + KAS coupling; kept-wins rule with Unassigned-adopt exception; held under `VesselDataUpdater.GetVesselLock` per M1 review; broadcasts `AgencyVisibilityMsgData` + `BackupSystem.RunBackup` on adopt branch per M2 review), **`AgencyWolfMigration` (Phase 4 Slice F — `CascadeOnDelete` invoked from `/deleteagency` BEFORE `TryDeleteAgency` to restore in-flight CrewRoute passengers from `RosterStatus.Missing` + respawn-MaxValue back to AC-pool `state = Available` + `ToD = 0` via depth-aware kerbal-file rewrite; restoration scope {Enroute, Arrived} per WOLF's Launch/CheckArrived state-machine contract; per-kerbal isolation; race-window mitigated by existing operator-/kick-first workflow)**. **AgencyState fields**: Funds/Science/Reputation + Contracts + TechNodes + ScienceSubjects + PurchasedParts + ExperimentalParts + Strategies + Achievements + FacilityLevels + KolonyEntries + PlanetaryEntries + OrbitalTransfers + Coverage + Scanners (Mod-compat S2 — body-keyed + vessel-keyed; the Scanners entry carries nested `List<AgencyScannerSensorRecord>` per Decision §9 multi-Sensor-per-Vessel) + DMagicAsteroidScience + DMagicAnomalies (Mod-compat S4 — Title-keyed asteroid science; composite "$bodyIndex|$name"-keyed anomaly records flattening DMagic's 2-level nested wire shape per Decision §B) + **WolfDepots (body+biome keyed) + WolfRoutes (4-string composite key) + WolfHoppers (Guid `D` form) + WolfTerminals (Guid `N` form) + WolfCrewRoutes (Guid `N` form; passengers carry nested kerbal Name/DisplayName/IsTourist/Occupation/Stars per Phase 4 Slice A)**. |

Vessel-side ingest lives under `Server/Message/` (proto, position, flight state, part sync, fairings, action groups, eva, decouple/couple). 30+ Harmony patches on the client mirror these.

Disconnect-driven outbound messages live in `Server/Client/ClientConnectionHandler.cs` — the `PlayerConnectionLeave` broadcast and the new `VesselPinned` broadcast (BUG-010) both fire from there before the lock-release storm.

## Web Dashboard (`Server/Web/`)

uhttpsharp HTTP server on port 8900 (configurable via `WebsiteSettings.Port`). Read-only JSON endpoints — there is no admin write path yet. Disabled by setting `WebsiteSettings.EnableWebsite = false`.

| Route | Content-Type | Payload | Refresh |
|-------|--------------|---------|---------|
| `GET /` | `application/json` | `ServerInformation` — current players, vessels, subspaces, settings | Polled every `WebsiteSettings.RefreshIntervalMs` (default 5000ms) by `RefreshWebServerInformationAsync` |
| `GET /fork` | `application/json` | `ForkInformation` — `ForkName` + `ProtocolVersion` + `ActiveFixes[]` (Stage 3.7) | Static — built per request from `ForkBuildInfo` |
| `GET /log` | `text/plain` | Human-readable log dump: `# fork / # protocol / # fixes / # ring` header followed by `LogRingBuffer` entries oldest-first. Operator-friendly in a browser tab. Formatter at `Server/Web/Formatting/LogTextFormatter.cs`. | Snapshot per request |
| `GET /logjson` | `application/json` | `LogSnapshot` — `Capacity` + `Count` + `Entries[]` for tooling that wants the structured fields | Snapshot per request |

Add a new JSON endpoint by registering one more `.With("route", new JsonGetHandler(() => new Payload()))` chain in `WebServer.StartWebServer`. For plain-text endpoints use `TextGetHandler` with a `Func<string>` factory. Prefer either over the `RestHandler<T>` + `IRestController<T>` pair for read-only routes — the REST pair is five per-endpoint methods, mostly `MethodNotAllowed` boilerplate.

## Settings (`Server/Settings/Definition/`)

12 XML-serialized settings groups:
- `GeneralSettings`, `ConnectionSettings`, `GameplaySettings`, `CraftSettings`, `DebugSettings`, `DedicatedServerSettings`
- `IntervalSettings`, `LogSettings`, `ScreenshotSettings`, `WarpSettings`, `WebsiteSettings`, `MasterServerSettings`

New settings: add a field with `[XmlElement]` and a default value, then verify round-trip via `SettingsHandler.LoadSettings` / `SaveSettings`. **Caveat:** `SettingsHandler.HasDifferencesAgainstGivenSetting` reflects over EVERY public property when validating against a difficulty preset (`SetEasy/SetNormal/SetModerate/SetHard`); a stored value that differs from the preset baseline silently flips `GameDifficulty=Custom`. For a setting that's intentionally orthogonal to difficulty (Stage 5's `PerAgencyCareer` is the first one), either accept the flip-to-Custom as documented behaviour or factor an exclusion mechanism. Deferred to the Stage 5.18 area — see `docs/research/05a-stage5-progress.md` "Deferred items".

## Admin Commands (`Server/Command/Command/`)

23 commands registered in [`CommandHandler.cs:RegisterCommands`](Server/Command/CommandHandler.cs): `backup`, `ban`, `changesettings`, `cleancontracts`, `clearvessels`, `connectionstats`, `countclients`, `dekessler`, `deleteagency`, `help`, `kick`, `listagencies`, `listclients`, `listlocks`, `nukeksc`, `restartserver`, `say`, `setagency`, `setfunds`, `setscience`, `setvesselagency`, `transferagency`, `vessel`.

Per-agency-career family (Stage 5.18d + Phase 3 closer):
- `listagencies` (5.18d) — read-only registry dump
- `setagency funds|science|reputation <token> <amount>` (5.18d) — scalar mutation
- `transferagency <agency> <new-owner>` (5.18d) — owner-RENAME only; preserves AgencyId + vessel stamps
- `deleteagency <agency> --confirm` (5.18d) — destructive, demotes vessels to Unassigned sentinel
- `setvesselagency <vessel-guid> <agency-token>` (Phase 3 Slice E-2, MKS-aware) — REASSIGNS a single vessel's `lmpOwningAgency` stamp; migrates per-router MKS partitions per pre-spec §4.e (kolony entries MOVE; orbital transfers MOVE if vessel is Destination, KEEP-in-source if Origin-only; planetary entries NO-MIGRATE per Q2); releases stale vessel-scoped locks; broadcasts `AgencyVisibilityMsgData` to all clients; scans third-agency orbital transfers for stranded cross-references

Mutating commands should default to non-destructive (precedent: `BackupCommand` requires explicit subcommands).

---

## Test Suite

`ServerTest/` (529 tests on `net10.0` via MSTest — pre-Stage-5 baseline 87, accumulated across Stage 5.14a → 5.18d + MKS Phase 3 merge + BUG-038 / BUG-039 + Mod-compat S2 (+31) + Mod-compat S4 (+22) + Mod-compat S1 (+10 this commit; S3 retired as no-work-owed 9404bfae) — all on `feature/per-agency`):
- `FileHandlerTest` — disk-IO round-trip
- `HandshakeSystemValidatorTest` — handshake validation
- `LockSystemTest` — lock state transitions + cross-subspace acquire rejection (BUG-005/006)
- `LunaMathTest` — math utilities
- `VesselStoreSystemTest` — vessel store invariants
- `VesselTest` — vessel object behavior
- `LogTest` — `LogEntry.Parse` + `LogRingBuffer` (Stage 1.2)
- `WarpRequestCacheTest` — `(player, seq)` dedup cache (BUG-051a)
- `WarpSoloDetectionTest` — solo-subspace transition logic (BUG-001)
- `VesselAuthorityTest` — `AuthoritativeSubspaceId` round-trip, `IsStrictlyPast`, `RemoveSubspace` vessel-auth guard (BUG-005/006)
- `WebDashboardTest` — `ForkInformation` + `LogSnapshot` payloads (Stage 3.7)
- `VesselSanitizerTest` — `ModuleReactionWheel` `stateString` locale-normalisation (BUG-013, session 5)
- `ScenarioStoreBackupRaceTest` — `SerializeUnderWriterLock` contract under concurrent mutation + `GetSemaphore` idempotency + per-scenario distinctness (BUG-033, session 8)
- `FileHandlerAtomicWriteTest` — `WriteAtomic` rotate+rename + `ReadAtomic` canonical-then-bak fallback + concurrent-write contention (Stage 5.14c)
- `AgencyStateTest` — ConfigNode round-trip + GUID-N filename + invariant-culture doubles + brace-wrap tolerance + missing-AgencyId-throws (Stage 5.14c)
- `AgencySystemTest` — register/load/save lifecycle + heal-on-bak-recovery + idempotent re-register + concurrent-same-name-race + dual-mode-disabled no-op + `ValidateDisplayName` (Stages 5.15a + 5.15c)
- `LockSystemAgencyTest` — cross-agency rejection on vessel-scoped lock acquires + Unassigned-sentinel bypass + force:true does NOT bypass cross-agency + reject-when-vessel-not-in-store (Stage 5.17a)
- `AgencyScenarioProjectorTest` — Project per-key (Funding/RnD/Reputation) + ProjectForClient gate/Sandbox/missing-scenario/null-client/missing-agency bypass + invariant-culture defenses + nested-key safety (Stage 5.17c)
- `AgencyContractRouterTest` — `ReadContractState` (bare + brace-wrapped + missing-state + zero-bytes) + `Upsert` (new appends + existing replaces + defensive-copy + null-guard + NumBytes clamp) (Stage 5.17d)
- `VesselMsgReaderCrossAgencyTest` — 8 cases pinning `RejectIfCrossAgencyWrite` for all 11 relayed vessel-message types: cross-agency reject + same-agency pass + Unassigned-sentinel bypass + gate-off bypass + Sandbox-mode bypass + vessel-not-in-store bypass (intentionally differs from 5.17a lock-acquire defense — see helper XML) + requester-has-no-agency bypass + all-11-message-types gated (session 19 soak Finding 2)
- `AgencyScanRouterTest` (Mod-compat S2, session 34) — 12 cases pinning `AgencyScanRouter.TryRoute` early-return branches + internal `UpsertCoverageEntries` / `UpsertScannerEntries` helpers: gate=off / null-client / Sandbox-mode short-circuit (3); per-body upsert + per-entry isolation on missing Name + no-Progress-container no-op (3); per-vessel upsert with nested Sensor list + cross-agency rejection + Unassigned-sentinel-bypass + vessel-not-in-store DROP + malformed-guid isolation (5)
- `AgencyScanProjectorTest` (Mod-compat S2, session 34) — 6 cases pinning `SpliceSCANsatCoverageIntoScenario`: strip-then-splice for Progress + Scanners with M9 empty-container retention + SCANResources untouched (Decision §6) + root UI scalars untouched (Decision §7) + multi-Sensor nested round-trip through projection (Decision §9) + null-agency input-unchanged guard
- `AgencyStateSCANsatRoundTripTest` (Mod-compat S2, session 34) — 8 cases pinning per-agency persistence under a comma-decimal thread culture (BUG-013 / Invariant 9): full-Body field round-trip + optional-field (ClampHeight + LandingTarget) null-vs-populated preservation + opaque Map blob byte-equal round-trip + multi-Sensor nested round-trip + empty-Sensors round-trip + per-Sensor malformed-isolation + per-Vessel malformed-guid isolation + pre-S2 file forward-compat (no SCAN_COVERAGE / SCAN_SCANNERS nodes → empty dicts)
- `AgencyTransferAgencySCANsatMigrationTest` (Mod-compat S2 / Decision §3 + D3, session 34) — 6 cases pinning `AgencyScanRouter.MigrateForVesselTransfer`: vessel-with-multi-sensor moves intact + no-source-entry no-op + destination-collision source-wins + per-body Coverage untouched by migration + same-agency-helper-call no-op + Empty-vesselId no-op
- `AgencyDMagicRouterTest` (Mod-compat S4, session 34) — 10 cases pinning `AgencyDMagicRouter.TryRoute` early-return branches (gate-off / null-client / Sandbox short-circuit) + internal `UpsertAsteroidScienceEntries` (per-asteroid Title upsert + per-entry isolation on missing title + no-Asteroid_Science-container no-op) + internal `UpsertAnomalyEntries` (per-body 2-level nested parse + flat composite-key storage + outer per-DM_Anomaly_List isolation on malformed Body + inner per-DM_Anomaly isolation on missing Name + no-Anomaly_Records-container no-op). No cross-agency check (no vessel keying).
- `AgencyDMagicProjectorTest` (Mod-compat S4, session 34) — 6 cases pinning `SpliceDMagicScienceIntoScenario`: strip-then-splice on Asteroid_Science (with M9 empty-container retention) + strip-then-splice on Anomaly_Records + per-body DM_Anomaly_List group-by-BodyIndex emit (Decision §B — anomalies on 2 bodies emit 2 wrappers) + lowercase wire field-name verification (sci / Lat / Body) + null-agency input-unchanged + gate-off ProjectForClient pass-through + malformed-input no-throw
- `AgencyStateDMagicRoundTripTest` (Mod-compat S4, session 34) — 6 cases pinning per-agency DMagic persistence under a comma-decimal thread culture (BUG-013 / Invariant 9): asteroid full-fields round-trip + anomaly full-fields round-trip + multi-body composite-key round-trip + per-anomaly malformed-BodyIndex isolation + per-asteroid missing-Title isolation + pre-S4 file forward-compat (no DMAGIC_* nodes → empty dicts)
- `AgencyVesselCoupleReconcilerTest` (Mod-compat S1, session 35) — 10 cases pinning `AgencyVesselCoupleReconciler.Reconcile` rule table + dual-mode silence + race guards: gate=off / Sandbox-mode no-op (2); kept-not-in-store + merged-not-in-store defensive race guards (2); same-agency idempotent + both-Empty idempotent (2); kept-Unassigned-merged-tracked adopt-branch (1) + symmetric merged-Unassigned-kept-tracked no-mutation (1); both-non-Empty-differ cross-agency couple A-kept + B-kept (2). M1 (per-vessel `VesselDataUpdater.GetVesselLock` race fix) + M2 (`AgencySystemSender.BroadcastVisibilityChange` + `BackupSystem.RunBackup` on adopt branch) applied per multi-lens review.

`MockClientTest/` (67 tests on `net10.0` via MSTest):
- `HandshakeSmokeTest` — wire-end-to-end handshake proves the harness setup
- `Bug051aDedupTest` — duplicate-seq subspace mint suppression
- `Bug001SoloBroadcastTest` — solo→non-solo broadcast on occupancy transition
- `Bug005SubspaceRejectTest` — cross-subspace past-side reject + future-side accept
- `Bug010PinnedBroadcastTest` — server-broadcast `VesselPinned` for each lock-owned vessel on disconnect
- `Bug025RejectionTest` — server-side duplicate tech-purchase detection + rejection-back-to-sender (session 9)
- `AgencyHandshakeTest` — five end-to-end agency-registration cases (Stage 5.16a, session 13): auto-registered handshake delivers AssignedAgencyId+State + privacy-rule second-state-absent assertion, second player's handshake summary includes the first, CreateRequest("valid") persists to disk via the owner-only channel (asserts disk file content directly — registry-served LoadAgency would mask SaveAgency failure), CreateRequest("") rejects with Guid.Empty + non-empty Reason, PerAgencyCareer=false produces zero Agency traffic on handshake (dual-mode silence)
- `VesselOwningAgencyTest` — six end-to-end `lmpOwningAgency` cases (Stage 5.16b, session 13): gate-on first-proto stamps sender's agency, existing-owner preserved on re-proto from different agency, **pre-existing Unassigned-sentinel vessel keeps Empty under gate=on** (round-5 upgrade-lens regression test), gate-on adversarial-wire scrub (server overrides spoofed lmpOwningAgency with sender's authoritative value), gate-off clean proto leaves field absent (dual-mode silence), gate-off spoofed bytes pass through unchanged (documents the dual-mode no-op contract)
- `CrossAgencyLockRejectionTest` — three e2e cases for the LockSystem cross-agency guard (Stage 5.17a, session 13)
- `CrossAgencyVesselRelayTest` — five e2e wire-level cases for the VesselMsgReader cross-agency relay guard (Stage 5.17a write-path counterpart, session 19 soak Finding 2): cross-agency `VesselPositionMsgData` from Bob silently dropped (watcher sees no relay), same-agency Alice relay reaches watcher (positive control), Unassigned-sentinel vessel relays from any agency (spec §10 Q3), plus the consumer-lens-caught [MUST FIX] `BobCrossAgency_VesselRemove_IsDropped_VesselStaysInStore` (Bob can't delete Alice's pinned-but-unlocked vessel) and the server-systems-caught [CONSIDER→fix] `BobCrossAgency_VesselCouple_IsDropped_DominantUnchanged` (Bob can't take over Alice's dominant vessel via Couple)
- `AgencyScenarioProjectionTest` — three e2e cases: two-client distinct projection with cross-leak privacy assertions + gate-off pass-through + Sandbox bypass (Stage 5.17c, session 13)
- `AgencyContractRoutingTest` — seven e2e cases (Stage 5.17d, session 13): gate-on Active routes owner-only (no peer relay) + gate-on Offered stays in shared scenario (no owner echo, no peer relay) + gate-off existing-relay-path unchanged (dual-mode silence) + per-agency persistence round-trips through Universe/Agencies/ disk + mixed-batch classification + **shared-pool guid removal on Accept** (upgrade-lens regression: prevents peer agencies from re-Accepting the same Offered slot) + **catch-up on reconnect** (consumer-lens regression: returning owner receives their persisted contracts before any mutation, so Stage 5.18a client mirror doesn't land empty)

`LmpClientTest/` now (91 tests on `net472` via MSTest — pre-Stage-5 baseline 57, +5 from 5.18a, +12 from 5.18b, +17 from 5.18c):
- `CommonUtilTest` (Stage 4.10 scaffold, session 4)
- `PqsAlignmentDecisionTest` (BUG-008 Phase A, session 6; +15 cases for `IsSurfaceSituation` + `ShouldPackForLoad` from the 4a pack-on-load slice)
- `InterpolationCapDecisionTest` (BUG-003/004, session 9 — pins the cap multipliers + scaling)
- `SteadyStateRetryDecisionTest` (BUG-051b, session 9 — one case per AND-chain guard + tolerance edges + seq=0 sentinel)
- `AgencyMembershipDecisionTest` (Stage 5.18a + 5.18b, sessions 16 + 17 — 17 cases pinning `IsForLocalAgency` defensive filter + `TryParseAgencyId` ConfigNode-field shapes + `RecordOwnership` relay-safety preservation rule)
- `AgencyLabelFormatterTest` (Stage 5.18c, session 18 — 17 cases pinning every branch of `FormatVesselAgencyLabel` (vessel-absent / Empty-Unassigned / local / known-peer / known-peer-empty-DisplayName / known-peer-both-names-empty / late-joiner / null-others-dict / null-registry) + `IsDefaultDisplayName` (auto-registered match / custom / empty-display / empty-owner / null cases / case-sensitivity))

(BUG-010 disconnect pin broadcast lives in `MockClientTest/` — there is no pure unit-testable surface for it because the trigger is a Lidgren disconnect arrival on the receive thread.)

`LmpCommonTest/` (14 tests):
- `LunaNetUtilsTest`, `MessageStoreTest`, `TimeTests`
- `SerializationTests` — wire round-trip cases; Stage 5.15b added 6 Agency-message tests (Handshake, CreateRequest C→S, CreateReply success+failure, State) with field-equality assertions via shared `RoundTripServer`/`RoundTripClient` helpers; Stage 5.17d added 2 more (AgencyContract populated batch + empty batch) — exposed and fixed a latent `ContractInfo.GetByteCount` under-count (compressed wire bytes can exceed decompressed `NumBytes` by QuickLZ overhead)

Detailed per-test rationale for each `MockClientTest` and `LmpClientTest` case lives in the brief lists above. Detailed historical context — e.g. why each Bug00x test was written, what `MockClientTest` setup looks like end-to-end — is captured in `docs/research/04-mock-client-harness-design.md` and in each commit's body.

Gaps to close as we touch each subsystem: backup archive lifecycle, settings round-trip, `Share*` broadcast routing. `VesselFlightStateUpdate.MaxInterpolationDuration` still uses `double.MaxValue` for the future branch — same shape as BUG-003/004 on the position path, would re-use the existing `VesselPositionUpdate.ComputeMaxInterpolationDuration` helper unchanged. KSP-bound coroutines like `PqsAlignmentRoutine`'s poll loop have to be soak-validated in-game.

---

## Key Design Decisions

| Decision | Choice | Reasoning |
|----------|--------|-----------|
| Transport | Lidgren UDP, custom messages | KSP needs low-latency vessel sync; HTTP/WS doesn't fit the cadence |
| Server framework | `net10.0` (was net5/Core in upstream) | Modern .NET, async/await ergonomics, current LTS-ish |
| Client framework | .NET Framework 4.7.2 | Required by KSP's Mono runtime; cannot upgrade without KSP itself changing |
| Career model | Shared-agency by default; per-agency on `feature/per-agency` | Original LMP design; per-agency is opt-in to avoid forking the community |
| Logging | `LunaLog` extends `BaseLogger` (LmpCommon) | Server adds file append, future ring buffer; client inherits same call surface |
| Persistence | Universe directory + ConfigNode files | KSP-native format; replayable via vessel proto files |
| Backup model | Two-tier: in-memory flush + archive snapshots | Operational safety without the cost of constant snapshotting |
| Harmony patches | One file per patched method | Surgical, reviewable, less merge conflict surface |
| AI attribution | Strip everywhere | Upstream community sensitivity (Fierce-Cat / #588) |
| Fork-upstream relationship | Fork-master only, no upstream coordination | Strategy shift 2026-05-16: AdmiralRadish's commits are reference material; we adopt/edit/replace case-by-case. Coordination meta-issue deferred indefinitely. |
| Protocol version | 0.30.0 (bumped from 0.29.1) | Cross-subspace lock keying (BUG-005/006) is a clean break; vanilla 0.29.x peers no longer cross-compatible. |
| Fork-local vessel metadata | `lmp*`-prefixed top-level ConfigNode fields | KSP vessel loaders ignore unknown fields; `lmp` prefix means our additions round-trip safely through any KSP-side persistence path. |

---

## Stack Notes & Patterns Learned

_Each entry has a date and the context that prompted it. Don't relearn these._

- **`RunBackup` is a flush, not a snapshot** (2026-05-16): existing `BackupSystem.RunBackup()` writes in-memory server state to the canonical Universe files. It is **not** a separate copy. The new `RunArchiveBackup()` (commit `f4aed253`) is the actual snapshot-with-retention path. Do not conflate them in naming, comments, or call graphs.
- **`LunaLog.Normal` is the project's info level** (2026-05-16): existing convention from `BackupCommand` and friends. Don't switch to `LunaLog.Info` for that role without a deliberate reason — `Info` exists but is reserved for emphasis (white text).
- **`LunaLog.LogFilename` is mutably reassigned across threads** (2026-05-16): `LogThread.cs` reassigns it on day rollover while `AfterPrint` reads it from network/system threads. Latent race; not currently observed, but don't make it worse without fixing it properly.
- **System `dotnet` is the wrong dotnet** (2026-05-16): typical Windows installs ship `dotnet` 7.x, which cannot satisfy the `10.0.100` pin in `global.json`. Use the user-installed `.NET 10 SDK` at `C:\Users\austi\.dotnet\`.
- **Singletons are heavily referenced but also heavily patched** (2026-05-16): `Funding.Instance` and friends appear ~83 times. Harmony interception is already a pattern (e.g., `ContractPreLoader_Filter.cs`). Per-agency work in Stage 5 means enumerating every read/write site and patching the singleton accessor — wide-but-shallow, not architecturally hard.
- **Upstream is actively revived by AdmiralRadish** (2026-05-16): 21 commits + 17 merged PRs since April 2026 across docking, coupling, scenario sync, lock handoff. Always `git fetch upstream` and check `upstream/master..HEAD` before touching those areas. We coordinate; we do not duplicate.
- **PlagueNZ split-progression fork is benchmark only** (2026-05-16): 113 commits ahead of upstream, 33 releases over 6 weeks, alpha-quality, bus factor 1. We compare our ground-up per-agency design against theirs but do not cherry-pick from them — the goal is to learn the architecture deeply, not inherit their decisions.
- **Fork-master strategy supersedes upstream coordination** (2026-05-16, session 3): all work happens on `master` of our fork. We observe `git log upstream/master --author=AdmiralRadish` for reference but do NOT post coordination issues up front. Decision per-fix: adopt his work verbatim, edit it, or replace it. Upstream PRs deferred until/unless explicitly revisited. The coordination meta-issue at `C:\tmp\luna-coordination-issue.md` is retained on disk but not posted.
- **Protocol bumped to 0.30.0** (2026-05-16, session 3, commit `d64acf66`): cross-subspace lock keying (BUG-005/006) restored the `SendUnloadedSecondary*` broadcasts that upstream `fbc7a8c` disabled. Mixing a fork 0.30.0 peer with a vanilla 0.29.x peer corrupts vessel state. The `(0,30,0,29)` cross-compat row was removed from `LmpCommon/LmpVersioning.cs`. Future protocol bumps need an equally significant break to justify.
- **`lmpAuthSubspace` is the canonical fork-metadata field on vessel ConfigNodes** (2026-05-16, session 3): the `lmp*` prefix means KSP's vessel loader silently ignores the unknown field, so our additions round-trip safely. Stored via the existing `MixedCollection<string, string> Fields` on `Server/System/Vessel/Classes/Vessel.cs`. Future fork-local vessel metadata MUST use the same prefix convention.
- **`Server/ForkBuildInfo.cs` is the registry of fork-applied fixes** (2026-05-16, session 3, commit `d2186e2e`): `ActiveFixes[]` lists every fix in commit-chronological order; `MainServer.Main` emits a `[fork] ...` banner at boot. Every runtime fix-related log line uses `[fix:BUG-XXX]` prefix so operators can `grep -F "[fix:"` to find fork-attributed events. When adding a new fix: append to `ActiveFixes[]` AND prefix the runtime log lines.
- ~~**LmpClient cannot be built locally without the .NET Framework 4.7.2 dev pack** (2026-05-16, session 3)~~ — **OBSOLETE 2026-05-16, session 4**: LmpClient now builds locally. Prereqs are documented in the "Build & Run → LmpClient" section: install the .NET Framework 4.7.2 Developer Pack and populate `External/KSPLibraries/` from a local KSP install (`KSP_x64_Data/Managed/`). `LmpClient/Directory.Build.props` wires `TargetFrameworkRootPath` so the SDK MSBuild finds the v4.7.2 targeting pack. Stage 2 client-side fixes that previously shipped "reviewed not compiled" (BUG-003/004 interp cap, BUG-051b retry, BUG-005/006 restored `SendUnloadedSecondary*`) are now compile-verified locally.
- **`Server/System/Vessel/VesselSanitizer.cs` is the central proto-vessel sanitiser** (2026-05-16, session 5, commit `c5ab8fa5`): defensive normalisation of inbound proto-vessel ConfigNodes runs inside `VesselDataUpdater.RawConfigNodeInsertOrUpdate` so neither the universe-on-disk copy nor any downstream relay carries the bad payload. Currently handles the BUG-013 reaction-wheel `stateString` locale problem; when a future KSP-localisation-bleed-through bug appears (BUG-013 family), add a new whitelist + module-name guard to this file rather than scattering one-offs. Idempotent on clean vessels; logs once per affected vessel with `[fix:BUG-013]`.
- **Vendored Lidgren has a real shutdown-race NRE** (2026-05-16, session 5, commit `b7a51ae1`): `NetReliableSenderChannel.DestoreMessage` dereferenced `storedMessage.m_recyclingCount` BEFORE its null check. A late ACK arriving while `NetPeer.ExecutePeerShutdown` was draining the heartbeat killed the host process with no diagnostic — caught only when the mock-client harness shut down NetServer + NetClient concurrently. Fix is local; the `Lidgren/` directory is fork-vendored and not synced from any active upstream, so the patch lives here. If we ever do sync from a maintained Lidgren fork, re-check this site.
- **Mock-client harness is single-instance per process** (2026-05-16, session 5): `ServerHarness` brings the Server up via static singletons (`ServerContext`, `WarpContext`, the 12 `*.SettingsStore`, the singleton `NetPeerConfiguration`). Only one harness instance per test process — use `[AssemblyInitialize]` / `[AssemblyCleanup]` boundaries. Per-test state resets in `[TestInitialize]` via `ServerHarness.ResetPerTestState` (currently clears `ServerContext.Clients`, `WarpRequestCache`, `VesselStoreSystem.CurrentVessels`, `ScenarioStoreSystem.CurrentScenarios`; resets `WarpSystem` + `AgencySystem`; forces `PerAgencyCareer=false`). Add further per-fix state clears there as new harness tests need them. **Do NOT** override `WarpContext.NextSubspaceId` — it races with `LoadSavedSubspace`'s pre-seed and causes silent `TryAdd` no-ops.
- **MockNetClient inbox preserves out-of-order messages** (2026-05-17, session 13, Stage 5.16a, commit `d9161c27`): `WaitForReply<T>` originally pulled messages one-at-a-time off a `BlockingCollection` and **discarded any whose Data type didn't match**. That worked by luck for the Stage 4 tests because each test waited for messages in the order the server happened to send them on a single channel. The Stage 5 agency wire spans multiple Lidgren channels (HandshakeReply ch 1 vs AgencyHandshake/State ch 22), and cross-channel arrival order is undefined — Lidgren's reliable-ordered guarantee is per-channel only. The new inbox is a thread-safe `List<IMessageBase>` + `ManualResetEventSlim` where `WaitForReply<T>` scan-and-removes only the first match; non-matching messages stay buffered for follow-up waits. **Add new tests by type, not by FIFO position** — multiple `WaitForReply<T>` calls return distinct T-typed messages, but a `WaitForReply<U>` between two `T` calls no longer drops the second T. Dispose invariant: do not call `Dispose` while a `WaitForReply` is in flight on another thread (existing tests honour this via single-threaded `using` blocks).
- **Strategy B port surface from `origin/Release/0_29_2` is heavily entangled** (2026-05-16, session 6): Drew Banyai's `Release/0_29_2` branch is the second active upstream workstream parallel to `upstream/master` (AdmiralRadish-led). Cherry-picking from it is rarely literal — Drew's commits build on each other: `36d06c89` (reason: wire field on VesselProtoMsgData) → `3d7f027a` (LocalTopologyTracker quarantine) → `18923984` (drift-dedupe cache) → `346ef48a` (per-tick proto budget + `VesselLoadOutcome` enum + `UpdateProtoInPlace`) → `4733081d` + `60a2ed5d` (`VesselSyncDiagnostics` with truncate-on-startup). Each later commit references symbols from the earlier ones. Strategy B landed BUG-045 (B.1) + per-tick budget (B.2, manual backport stripping LocalTopologyTracker / drift-dedupe / VesselSyncDiagnostics) + `VesselSyncDiagnostics` + Reason field (B.3) but **deferred** LocalTopologyTracker, drift-dedupe, server-side `CraftCreationAndRemovalLog`, and `VesselRemoveMsgData.Reason`. Recipe for future ports from this branch: read `git log origin/master..origin/Release/0_29_2 -- <target-file>` end-to-end first, identify upstream symbol dependencies, and prefer manual backport over `git cherry-pick` when prior-commit entanglement is non-trivial. The strategy doc at [docs/strategy-b-implementation-plan.md](docs/strategy-b-implementation-plan.md) was based on this fork's pre-Strategy-B understanding of phase isolation; the post-execution reality is that the original B.1 → B.2 → B.3 order is correct **provided B.2 is a manual backport, not a verbatim port**.
- **Disconnect-driven vessel pinning (BUG-010 Part A)** (2026-05-16, session 7): when a player disconnects, the server enumerates their Control + Update + UnloadedUpdate locks and broadcasts a new `VesselPinnedMsgData` (wire enum `VesselMessageType.Pinned = 15`, additive — no protocol bump) for each distinct vessel id **before** `LockSystem.ReleasePlayerLocks` fans out. Remaining clients hold those vessels immortal via `VesselPinnedSystem._pinnedVessels` until any player takes the Control or Update lock — either the original pilot reconnecting and re-asserting, or the local player switching to it (handled in `VesselPinnedEvents.OnLockAcquire`, fired by `LockEvent.onLockAcquire`). `VesselImmortalSystem.SetImmortalStateBasedOnLock` early-returns `SetImmortal(true)` for any vessel id in the registry, short-circuiting the lock-release → re-acquire → mortal-flip chain that was destroying floatplanes-on-water. Active vessel of the local player is never pinned (the merged-couple Variant B case). We deliberately do NOT hook `GameEvents.onVesselChange` as an early-unpin path — unpinning at vessel-switch time would call `SetImmortalStateBasedOnLock` before the local lock-acquire round-trip lands, flipping the vessel mortal for one or more physics ticks while the leaver's stressed pose is still settling. **Two important wire-symmetry gotchas:** (1) any new `VesselMessageType` value requires entries in BOTH `LmpCommon/Message/Server/VesselSrvMsg.cs` AND `LmpCommon/Message/Client/VesselCliMsg.cs` `SubTypeDictionary` — `MessageBase.GetMessageData` throws on unknown subtype and the receiver silently drops the deserialization failure. (2) `VesselSrvMsg.DefaultChannel` special-cases the Pinned subtype to ride channel 14 (the lock-message channel) instead of vessel channel 8, because Lidgren's reliable-ordered guarantee is per-channel and the pin must arrive at the client BEFORE the lock-release storm or the immortal-flip suppression doesn't engage in time.
- **Graceful-disconnect proto-flush (BUG-010 Part B)** (2026-05-16, session 7): `MainSystem.DisconnectFromGame` now calls `VesselProtoSystem.MessageSender.SendOwnedVesselsForDisconnect("graceful disconnect")` between `SendScenarioModules` and `NetworkConnection.Disconnect`. Iterates `FlightGlobals.Vessels`, filters to vessels where the local player holds Control or Update lock, and **synchronously** serializes + hands the bytes to Lidgren via `NetworkMain.ClientConnection.SendMessage` + a single `FlushSendQueue` at the end. The normal periodic-broadcast path (`SendVesselMessage`) is intentionally NOT used here because it offloads serialization to `TaskFactory.StartNew` and enqueues onto `NetworkSender.OutgoingMessages` — both of which race against `NetworkConnection.Disconnect → ResetConnectionStaticsAndQueues` (which wipes the queue microseconds later). When `SendOwnedVesselsForDisconnect` returns, every proto is in Lidgren's outgoing buffer and survives the wipe. Lingoona-safe because the caller is on the Unity main thread (per the historical comment on `PrepareAndSendProtoVessel`). **Does NOT run on ungraceful drops** (network died, process killed) and is **deliberately NOT wired into `OnExit`/`OnApplicationQuit`** — those paths fire during Unity teardown where `BackupVessel` interacts badly with KSP destroying GameObjects. Window-close-while-flying falls back to Part A pin protection + whatever pose was in the last periodic broadcast. Same trust pattern as upstream's `SendScenarioModules` flow (`6bb056ff`): reliable-ordered Lidgren guarantees the server processes the proto before the disconnect packet, no server ack message type needed.
- **`VesselSyncDiagnostics` is the client-side per-session vessel-event trace** (2026-05-16, session 6, B.3 commit): append-only pipe-delimited log at `{KspPath}/Logs/LMP/VesselSyncLog.txt`, truncated on every launch (same lifecycle as `KSP.log`). Records ARRIVED / DISCARDED / LOADED / RELOADED / SWAPPED / UNCHANGED / FAILED / REMOVED per vessel-proto wire event. Thread-safe — `Log*` methods are safe from network thread; scene is cached via `NotifyScene` from the Unity-thread routine in `VesselProtoSystem.CheckVesselsToLoad`. Drained on `MainSystem.OnExit`. Kill-switchable via `SettingsStructure.VesselSyncDiagnosticsEnabled` (default true). Self-disables on init or write failure — diagnostic instrumentation must never break what it observes. `SendVesselMessage` now takes a `string reason = null` parameter (and `VesselProtoMsgData.Reason` is the on-wire field, backward-compat via length-bits position check); the senderReason flows into the receiving client's ARRIVED line as `senderReason=...`. When adding a new `SendVesselMessage` call site, pass a short human-readable reason ("part decoupled (new vessel)", "EVA construction: part attached", etc.) so the trace stays grep-friendly.
- **`.github/workflows/build.yml` is the CI surface** (2026-05-17, session 8, Stage 4.11): GHA Ubuntu, `actions/setup-dotnet@v4` pinned to 10.0.x, matrix of four (component, project, test-project) entries — `server`, `masterserver`, `lmpcommon`, `mockclient`. Triggers: `pull_request` AND `push: branches: [master]` (the push trigger means direct commits to master get CI too, which they didn't before). LmpClient + LmpClientTest are deliberately NOT in the matrix because they need KSP-licensed Unity DLLs in `External/KSPLibraries/` that cannot be checked in — local-only test surface. **Master-server two-project split is intentional, not a typo:** `MasterServer/MasterServer.csproj` is an Exe entry-point shell (`OutputType=Exe`, only `ProjectReference` entries to `LmpMasterServer` + `LmpUpdater`, no logic of its own); `LmpMasterServer/LmpMasterServer.csproj` is the `Library` that carries the HTTP server, MaxMind GeoIP, and all real dependencies. The `masterserver` matrix entry therefore correctly builds the Exe wrapper (which transitively pulls the library) and tests `LmpMasterServerTest` against the library. Don't "clean this up" without confirming the Exe-shell vs library split isn't load-bearing for the deployment story. When you add a new test project, append one matrix entry (no other CI changes needed). Mock-harness flakes (the `Bug001SoloBroadcastTest.SoloDetected_BroadcastsToConnectedClient` ~1/3 flake observed locally on this workstation) are watched-in-CI for now; if Ubuntu also flakes, the response is to fix the harness's timing, not retry-loop the test.
- **`ScenarioDataUpdater.GetSemaphore` is the lock anchor for per-scenario writes AND backup-side serialization** (2026-05-17, session 8, BUG-033 commit `87105f41`): every `ScenarioDataUpdater.Write*DataToFile` writer holds `Semaphore.GetOrAdd(scenarioName, new object())` before mutating the named scenario's `ConfigNode`. `ScenarioStoreSystem.BackupScenarios` now takes the SAME per-scenario lock around its `ConfigNode.ToString()` call via the new `internal` accessor `ScenarioDataUpdater.GetSemaphore(name)`, so the iterator inside `ToString` does not collide with an in-flight `AddNode`/`RemoveNode`/`ReplaceNode`. The disk write is performed OUTSIDE the lock so I/O latency does not extend the writer-blocking window. **Do NOT** add an outer `BackupLock` back around the per-scenario acquisitions: it would deadlock against `ScenarioPartPurchaseDataUpdater.WriteExperimentalPartDataToFile`, which holds `Semaphore["ResearchAndDevelopment"]` and recursively calls `BackupScenarios` (classic AB-BA cycle). The startup-only `LoadExistingScenarios` and `ChangeExistingScenarioFormats` keep their `BackupLock` (mutual exclusion against each other, no overlap with the backup hot path). When you add a new writer in `Server/System/Scenario/`, follow the existing `lock (Semaphore.GetOrAdd(name, new object()))` pattern — the backup-side serialization will pick up the same lock automatically because `ConcurrentDictionary.GetOrAdd` is idempotent on the key.
- **Server-side check-and-claim with rejection-back-to-sender is the BUG-025 fix template** (2026-05-17, session 9, commits `83905d4d` + the v2 follow-up): when a client commits a shared-state change LOCALLY (deducts science, charges funds, etc.) and then broadcasts, the server needs a synchronous check that can REJECT and tell the sender to refund. The pattern: synchronously check existing state under the per-scenario writer lock; if duplicate, send a server-to-client rejection message (additive enum value, no protocol bump) carrying the refund amount; the client's message handler refunds via the matching `Add*` API — **wrap the refund in `ShareXxxSystem.Singleton.StartIgnoringEvents() / StopIgnoringEvents()` so the resulting `OnXxxChanged` event doesn't fire `ShareXxxEvents.XxxChanged` which would broadcast our new total back to the server, fanning out to every peer and overwriting THEIR totals**. The original BUG-025 v1 shipped without the bracket and the independent client-harmony-review caught the science-feedback loop — this pattern applies to any future Share* fix that does local-commit-then-broadcast.
- **`VesselLoader.ScrubInvalidProtoCrew` is the load-time scrubber for null `protoModuleCrew` entries** (2026-05-17, session 9, BUG-023 commit `5a240c32`, ported from Drew Banyai's `origin/Release/0_29_2` 138c2b3e): stock KSP appends null to `ProtoPartSnapshot.protoModuleCrew` when a wire-side `crew = NAME` value doesn't resolve in `HighLogic.CurrentGame.CrewRoster` — typical during the initial-sync burst where `VesselProto` (channel 8) can beat `KerbalProto` (channel 16) for the same kerbal. Three stock paths NRE on the null: `KbApp_VesselCrew.CompareSeatIdx` (frozen Tracking-Station info pane = the AC desync symptom), `Part.RegisterCrew` (black flight scene), `ModuleCommand.UpdateControlSourceState` (every-FixedUpdate NRE storm). The scrubber removes null entries from `protoModuleCrew` IN LOCKSTEP with `protoCrewNames` — non-lockstep removal would cause `KerbalRoster.ValidateAssignments` to `ExpungeKerbal` an unrelated real kerbal at the shifted index on the next save/load cycle. Pairs with the new `VesselProtoSystem.CheckVesselsToLoad` drain (calls `KerbalSystem.LoadKerbalsIntoGame()` before each vessel-load batch) + two Harmony patches (`Part_RegisterCrew` prefix + `KnowledgeBase_GetVesselCrewByAvailablePart` postfix) for the autosave Save+Load re-introduction path. When you next add a sender that creates ProtoPartSnapshot data with crew references, mirror this defensive surface — the wire-side crew-name resolution is fundamentally racy because Lidgren's reliable-ordered guarantee is per-channel.
- **BUG-008 pack-on-load is gated by `ShouldPackForLoad`, never on the active vessel** (2026-05-17): `PqsAlignmentRoutine.AlignAndThen` packs a freshly-loaded surface vessel (LANDED/SPLASHED/PRELAUNCH) only when it arrived in physics range (`vessel.packed == false`) on a body with a `pqsController`, AND it is not `FlightGlobals.ActiveVessel`. The active-vessel exclusion is non-negotiable — packing the camera-target judders the player's view, and the active-vessel reconnect case is a separate gap that needs phantom-force suppression (BUG-008 item 4c) or a hard landed-pin (4d) instead. Already-packed remote vessels stay on the snap-only path because frozen physics can't scramble polygons; only the `packed==false` case has the collider race. After the pack the coroutine polls PQS until stability (or the existing 5s cap), snaps if `NeedsRealignment`, yields one FixedUpdate to let `Vessel.UpdateCaches` settle on the new pose, then unpacks. The unpack is wrapped in a try/catch — if KSP refuses, the vessel is left packed and KSP's natural physics-range tick re-evaluates on the next FixedUpdate. Log marker is `[fix:BUG-008-pack]` (distinct from the `[fix:BUG-008]` snap-path marker) so operators can grep pack-path events independently of snap-path events.
- **Pure-helper extraction for client-internal decision math is the Stage 4.10 pattern** (2026-05-17, session 9, commit `ecd9997f`): KSP-bound decision logic that needs LmpClientTest coverage gets pulled out of the inline call site into a `public static` helper that takes its inputs as parameters (not reads from singletons or `TimeWarp.*`). The instance method becomes a one-line delegate that captures the call-site state and passes it through. Pattern in production: `VesselPositionUpdate.ComputeMaxInterpolationDuration(int intervalMs, bool subspaceIsEqualOrInThePast)` for BUG-003/004; `WarpSystem.ShouldSteadyStateRetry(int currentSubspace, bool waitingSubspaceIdFromServer, int timeWarpRateIndex, float timeWarpRate, uint currentRequestSeq)` for BUG-051b; `PqsAlignmentRoutine.NeedsRealignment(...)` for BUG-008 Phase A. Promote any named constants the helper uses from `private const` to `public const` so test cases can pin them — they're solution-internal so the const-inline-into-callers ABI risk is theoretical. When extracting, verify behaviour preservation bit-for-bit (no sign flip, no precision shift) BEFORE writing tests; the tests pin the new helper, so they cannot detect a regression introduced during extraction.
- **`feature/per-agency` is fork-divergent — never push to upstream from it; nothing merges back to `master` until Stage 5 acceptance** (2026-05-17, session 10, branched from `master` at `6515e006`): Stage 5 per-agency career lives on this branch. All Stage 5 commits land here including CLAUDE.md edits + progress-tracker doc. Master receives only fork-master work (soak hotfixes, BUG-008 4b/c/d if needed, unrelated fixes); when master moves, merge master → feature/per-agency (not the other way) until Stage 5 ships. Two non-obvious gates the audit at [docs/research/05a-plaguenz-audit.md](docs/research/05a-plaguenz-audit.md) surfaced and that must be decided BEFORE Stage 5.14 code starts: (1) **server-side scenario projection vs. client-side Harmony patching** — PlagueNZ proved scenario projection alone covers funds/science/rep/tech, deleting most of the ~83-site Harmony work the spec assumes; (2) **contract architecture is hybrid (shared offered pool + per-agency Active/Completed)**, not full-isolation — PlagueNZ shipped full-isolation and bailed under Contract Configurator interop pain. Plus (3) the soak window for v0.30.0-private-1 needs ≥48-72h of cohort signal before Stage 5.14 begins. Tracker at [docs/research/05a-stage5-progress.md](docs/research/05a-stage5-progress.md) is the per-step source of truth on this branch.
- **Per-agency wire surface anchored at `AgencySrvMsg` ch 22 / `AgencyCliMsg` ch 21** (2026-05-17, session 12, Stage 5.15b + 5.15c): `LmpCommon/Message/Data/Agency/` holds the per-agency MsgData family. `ServerMessageType.Agency=21` + `ClientMessageType.Agency=20` route inbound CliMsgs to `Server/Message/AgencyMsgReader.cs` via `MessageReceiver.HandlerDictionary`. Outbound is `Server/System/Agency/AgencySystemSender.cs`. **Privacy rule (spec §10 Q1):** `AgencyStateMsgData` carries Funds/Science/Reputation and is therefore OWNER-ONLY — never `SendToAllClients`. Cross-agency awareness goes through the public-summary `AgencyInfo` struct embedded in `AgencyHandshakeMsgData` (id + owner + display name, no scalars). The 5.15b registration set ships 4 messages (Handshake/CreateRequest/CreateReply/State); the remaining 9 mutation+visibility messages from spec §4 are deliberately deferred to their consumer steps (5.17b/5.17d/5.18c) so wire definitions land with their callers. See [[reference-agency-wire-extension]] memory for the 6-step recipe each of those steps will run.
- **`AgencyState` locking contract for Stage 5.17b Share* writers** (2026-05-17, session 12, Stage 5.15c): mutations to any `AgencyState` field MUST hold `AgencySystem.GetAgencyLock(agencyId)` around the write. `SaveAgency`'s `Serialize` acquires the same lock — without the caller-side hold, a multi-field mutation (e.g. paying for a tech node = debit Science + flip TechNodeState) produces a torn intermediate snapshot on disk. `GetAgencyLock` is `internal` and ServerTest-visible; expose as `public` when the client-side mirror lands in 5.18a. Same shape as `ScenarioDataUpdater.GetSemaphore` from BUG-033 — the per-key lock + caller-cooperation contract is the established LMP pattern. `RegisterAgency` separately uses a per-player-name lock (closes the same-name concurrent-register race the pass-1 review caught); these two lock families are orthogonal and acquired in a consistent Name→Agency order, no AB-BA cycle.
- **`lmpOwningAgency` first-owner-wins applies ONLY to genuinely new vessels; pre-existing vessels (including the `Guid.Empty` Unassigned sentinel) are sticky** (2026-05-17, session 13, Stage 5.16b, commit `5499101d`): `VesselDataUpdater.RawConfigNodeInsertOrUpdate`'s gate-on stamp block has three branches: (a) `existingStored != null` → preserve `existingStored.OwningAgencyId` (including Empty, which is the spec §10 Q3 Unassigned sentinel for pre-0.31 vessels — never silently re-stamp), (b) genuinely new vessel + sender has agency → stamp sender's agency, (c) new vessel + no sender agency → scrub to `Guid.Empty`. The Round-5 upgrade-lens review caught a bug where branch (a) only fired for non-Empty existing values; branch (b) was running on pre-existing-but-Unassigned vessels, mass-assigning each one to whoever sent the first proto. Spec §10 Q3 mandates admin-only transfer (Stage 5.18d `transferagency`). When you next change the stamp logic: pre-existing vessels are ALWAYS preserved regardless of their stamp value; the only path that mutates an existing vessel's OwningAgencyId is the future admin command. **Server-stored copy is authoritative; relayed proto bytes carry the wire-supplied value unchanged** — Stage 5.18a client mirrors MUST re-derive ownership from `VesselSync` replies (which serialise from the authoritative store via `GetVesselInConfigNodeFormat`) or from Stage 5.18c `AgencyVisibilityMsgData`, never from the relayed proto bytes. LockSystem (5.17a) reads from the authoritative store so cross-agency lock decisions are always safe regardless of relay payload.
- **Lens-framing matters more than reviewer count for security/correctness reviews** (2026-05-17, session 13, Stage 5.16): five rounds of independent review on Stage 5.16a+b caught 7 real production bugs. The breakdown is instructive: Rounds 1-4 (general / server-systems / network / persistence / convergence lenses) caught issues during the implementation phase — dual-mode silence violation, RegisterAgency mutation ordering, OOM DoS, BackupSystem snapshot scope, wire desync regression. Round 5 (consumer-lens for the Stage 5.18a future client author + upgrade-lens for the operator upgrading a pre-0.31 universe) caught TWO MORE prod bugs the prior four rounds missed: the pre-existing-vessel first-proto-wins regression, and the silent CreateRequest drop under gate=off. **Neither was a general-correctness issue; both were "what does the consumer / what does the upgrader experience" issues.** Lesson for future stages: when reviewing a wire surface, explicitly run a "what would a downstream consumer hit" pass. When reviewing a data-format change, explicitly run a "what would loading existing data hit" pass. These two lenses have to be deliberately chosen; a general "is the code correct" reviewer will pass code that's correct in isolation but breaks contracts with downstream/upstream. See `[[feedback-review-lens-framing]]` memory for the operational guidance.
- **Per-agency contract routing must free the shared Offered slot on Accept** (2026-05-17, session 13, Stage 5.17d): `AgencyContractRouter` routes Active/Completed/etc. to per-agency `AgencyState.Contracts` and never updates the shared `ContractSystem` scenario. But the shared scenario still holds the Offered version of the contract (CC seeded it) — without an explicit removal, the next `SendScenarioModules` ships that Offered entry to every other agency, who could "accept" the same contract independently and produce duplicate per-agency claims of the same Offered slot. Fixed via `ScenarioDataUpdater.RemoveContractFromSharedOfferedPool(Guid)` called from `AgencyContractRouter.ApplyPerAgencyBatch` after upsert. The upgrade-lens reviewer caught it; the general-correctness lens missed it because the in-memory routing is internally consistent — the bug only surfaces when a SECOND agency observes the still-Offered scenario. When extending the router (5.17b for tech / facility / kerbal / strategy if they have a shared pool model), use the same pattern: any per-agency route that consumes a shared-pool slot must explicitly free that slot.
- **Consumer-lens reviews want explicit "when does this message arrive at the client" docs** (2026-05-17, session 13, Stage 5.17d): the Stage 5.17d consumer-lens review identified that a 5.18a author reading `AgencyContractMsgData.cs` couldn't tell from the doc-comments alone (a) whether it's push-only mid-session or also catch-up at connect, (b) whether the client write path is the SAME wire (`ShareProgressContractsMsgData`) or a new C→S agency message, (c) where reward routing happens. Add these three "consumer experience" facets to every owner-only S→C message: ARRIVAL CONDITIONS (on connect? mid-session? both?), CLIENT WRITE PATH (does the client send this back, or send a different message?), and ORTHOGONAL CONCERNS (what reward / state-machine bits flow through OTHER messages, NOT this one). Closed for AgencyContractMsgData; the same shape applies to the remaining 8 deferred Agency*MsgData messages landing in 5.17b/5.18c. See `[[reference-agency-wire-extension]]` recipe — append a 7th step: "document arrival + write-path + orthogonal concerns on the class XML."
- **Pre-0.31 upgrade lens needs FOUR separate "would the operator lose data here?" diagnostics** (2026-05-17, session 13, Stage 5.17d): the upgrade-in-place hazard inventory has grown across 5.17c → 5.17d. (1) Career scalars vanish on first agency mint — `WarnAboutSavingsLossOnUpgrade` (5.17c). (2) Vessels missing `lmpOwningAgency` need the Unassigned sentinel — handled in branch (a) of `VesselDataUpdater` first-stamp logic (5.16b). (3) Shared `ContractSystem` Active/Finished pools leak to every per-agency client until the projection lands — `WarnAboutSharedContractsOnUpgrade` (5.17d). (4) Tech tree / facilities / kerbal roster have analogous risks and will need the same treatment when 5.17b lands their per-agency routing. Each new per-agency career resource must ship its own upgrade-lens diagnostic at boot — the operator workflow (`fresh-start-only` per spec §10) is permissive about omitting these, but operators who skip the docs only learn about the loss after it's irrecoverable.
- **Relayed vessel-proto bytes are advisory, not authoritative — never populate client-side state from them without a preservation rule** (2026-05-18, session 17, Stage 5.18b): the server's vessel-relay path forwards the ORIGINAL sender wire bytes (see `Server/Message/VesselMsgReader.cs:188-198`), but KSP's `BackupVessel`/`protoVessel.Save` strips unknown top-level fields like `lmpOwningAgency` on every local-owner resend. Result: every periodic drift-correction resend arrives at peer clients with the field absent. A naive `dict[vesselId] = parsedValue` on the client side downgrades real agency ids to `Guid.Empty` on every resend — the 5.18b initial implementation had this bug; the consumer-lens review caught it because the server-side relay-vs-store comment explicitly warned about it. The fix is `AgencyMembership.RecordOwnership` — non-Empty incoming writes through; Empty incoming inserts only when absent, never downgrades. Authoritative writes come from VesselSync replies (server-side `GetVesselInConfigNodeFormat` DOES serialize the field) and the future 5.18c `AgencyVisibilityMsgData`. **General principle for any future fork-local vessel-metadata client mirror:** the relay path is lossy; design the registry's write semantics around "wire-source non-Empty wins; wire-source Empty preserves prior" or use a dedicated authoritative-only channel that bypasses the relay. The Stack Notes entry on `lmpAuthSubspace` (server-stamped, not client-stamped) is the precedent for "server is the canonical source." See `[[feedback-research-first]]` — I would not have caught this myself without reading the existing server-side warning comment.
- **Server-relay path needs an explicit cross-agency write-side guard — the 5.17a lock-acquire defense does NOT cover the broadcast path** (2026-05-18, session 19, Stage 5.17a write-path counterpart, soak Finding 2): KSP's tracking-station "Fly" loads ANOTHER player's vessel into the local Flight scene and makes the local client the active simulator for it. The client then broadcasts `VesselPositionMsgData` / `VesselFlightStateMsgData` / etc., and the server's per-message relay in `Server/Message/VesselMsgReader.cs` forwarded them UNCONDITIONALLY (only `RejectIfPastSubspace` gated). The owning agency's authoritative simulation then collided with the relayed state = physics jitter on the owner's instance. 5.17a's `LockSystem.AcquireLock` guard closes the lock-taking hole but NOT the broadcast hole — a non-owner can broadcast without holding any lock at all. Fix: new `RejectIfCrossAgencyWrite` helper, called from the 11 relayed vessel-message cases (Position / Flightstate / Update / Resource / PartSyncField / PartSyncUiField / PartSyncCall / ActionGroup / Fairing / Decouple / Undock) after `RejectIfPastSubspace`. **Consumer-lens + server-systems reviewers caught two adjacent holes the same guard closes:** `HandleVesselRemove` was only gated on `ControlLockExists` (and bypassed entirely when no Control lock was held — Alice's pinned-but-unlocked vessel could be deleted by cross-agency Bob); `HandleVesselCouple` rewrites the dominant's AuthoritativeSubspaceId + removes the weak vessel + broadcasts a remove with no cross-agency check (Bob could take over Alice's dominant vessel by initiating a couple). All three call sites now run `RejectIfCrossAgencyWrite(client.PlayerName, data)` before any mutation. Same bypass semantics as 5.17a (gate off, Unassigned sentinel, requester has no agency) — EXCEPT the vessel-not-in-store branch is BYPASS (not reject) because the relay-path ingest race is asymmetric (a peer needs the vessel id, which means relay already happened). Log at `Warning` level (3/3 reviewers flagged Debug as operator-invisible) — KSP-side cadence might require rate-limiting later; bias toward visibility for now. Pinned in `VesselMsgReaderCrossAgencyTest` (8 cases) + `MockClientTest/CrossAgencyVesselRelayTest` (5 cases — incl. Remove + Couple). No lock-bypass branch in the helper because 5.17a structurally prevents cross-agency lock holders under gate=on. **Proto is intentionally excluded** — it has its own ownership-stamping logic and the established contract that relayed bytes are advisory ([[5.18b relay-vs-store note]]). **When adding new vessel-message handlers that mutate or relay, register them in the same `RejectIfPastSubspace + RejectIfCrossAgencyWrite + ...` sequence** — the helper is type-agnostic and reads only the base `VesselId`.
- **`StringWriter.Encoding` is hard-coded to UTF-16 and lies about what gets written to disk** (2026-05-19, BUG-038 fix `LmpCommon/Xml/LunaXmlSerializer.cs`): `StringWriter` overrides `TextWriter.Encoding` to always return `Encoding.Unicode` (UTF-16) regardless of the surrounding pipeline's actual target encoding. `XmlSerializer.Serialize(TextWriter)` and `XmlDocument.Save(TextWriter)` both inspect that property when writing the `<?xml version="1.0" encoding="..."?>` prologue, so any code that pipes XML through a vanilla `StringWriter` and then writes the resulting string to disk via `File.WriteAllText` (which is UTF-8 no-BOM by default in .NET) produces files whose declared encoding disagrees with the actual bytes. The fix shape: a private `Utf8StringWriter : StringWriter` nested in the serializer, overriding `public override Encoding Encoding => Encoding.UTF8;`, used at every `new StringWriter()` site in the prologue-writing path. Same fix has to be applied to any code that duplicates that pipeline — `Tools/AdminGui/LunaServerGui/Services/SettingsXmlService.cs` is a deliberate byte-equivalent port and silently re-introduced the bug from the operator-edit side until BUG-038 was closed symmetrically. When you next port XML serializer logic into a new project, audit every `StringWriter` site.
- **`git merge-tree` on diverged branches tells the truth; memory's textual-conflict prediction can over-count** (2026-05-18, session 32, MKS Phase 3 merge `d4ff0511`): the pre-merge `[[project-mks-merge-to-per-agency]]` memory predicted 4 textual conflicts (CLAUDE.md + SerializationTests.cs + AgencyScenarioProjector.cs + AgencySystem.cs). The actual probe via `git merge-tree --write-tree --name-only --merge-base <base> <branch1> <branch2>` returned only 2: CLAUDE.md + SerializationTests.cs auto-merged cleanly at the hunk level even though both sides edited them. **Lesson:** before writing a merge breakage analysis, ALWAYS run `git merge-tree` to get the actual conflict surface; don't trust a memory's prediction without re-probing. The two real conflicts both resolved as "take-both for non-overlapping additions to the same code structure" — splice-keys array + switch table + per-method body extensions in AgencyScenarioProjector.cs; new `WarnAbout*OnUpgrade` methods + RefuseStartup predicates in AgencySystem.cs. For the AgencySystem.cs case (mks-side massive +640 lines vs per-agency +85 lines confined to one method), the cleanest resolution was `git checkout --theirs` + a single `Edit` to patch the per-agency-side's refreshed `WarnAboutSharedContractsOnUpgrade` method back in — vs trying to hand-merge a ~300-line conflict block. **Recipe for adjacent take-both conflicts:** if one side's diff is overwhelmingly bigger AND the smaller side's changes are confined to a small region, checkout-theirs + patch-back-the-smaller-side is faster + less error-prone than line-by-line resolution. Verified post-merge with R1-R5 invariant grep checks + general+integration-logic lens reviews (0 MUST FIX). See [[project-mks-merge-to-per-agency]] for the worked example.
- **WebFetch-based mod audits drift; pre-implementation source re-walks catch the drift** (2026-05-19, session 34, Mod-compat S2 audit re-walk): the 2026-05-18 SCANsat audit (WebFetch from `KSPModStewards/SCANsat`) had three structural errors that would have produced an implementation-wrong router/projector: (1) flat `Scanners → Vessel` shape where the real source nests 1-N `Sensor` children inside each Vessel; (2) only two root containers enumerated (Progress + Scanners) where the real source has three (Progress + Scanners + SCANResources); (3) field-name confusion (`BodyName` vs `Name`, `body_scan` vs `Map`, separate lat/lon vs combined `LandingTarget`, lowercase `guid`/`name`). Catching this caught the [[feedback-audit-via-prespec]] discipline working as intended — scaffolding-first pre-implementation pre-spec exposed the audit-derived shape didn't match the source. **Recipe for future WebFetch-based mod audits:** before implementing a router/projector, shallow-clone the upstream repo to `F:/tmp/mks-external/<modname>/` (per [[reference-mks-external-clones]] precedent) and re-walk the actual source — especially OnSave/OnLoad bodies + any helper methods that produce persisted strings (e.g. `saveResources(SCANresourceGlobal r)` was the source of the `MinMaxValues` shape). The 30-min re-walk recoups multi-hour mis-implementation costs. Per-agency S2 corrections folded into `docs/mod-compat/SCANsat.md` "Re-walked 2026-05-19" subsection + Decisions §6/§7/§8/§9 (commit `2efe5f85`) + scrub commit (`5a0171fb`) overwrote stale audit tables so future readers can't land on outdated claims.
- **Path B router suppression model is invisible to gate=off** (2026-05-19, session 34, Mod-compat S2 first slice): under `PerAgencyCareer=on` + Career mode, `AgencyScanRouter.TryRoute` returns `true` and the caller (`ScenarioBaseDataUpdater.RawConfigNodeInsertOrUpdate`) SKIPS the shared-store `AddOrUpdate("SCANcontroller", ...)` entirely. The server's `CurrentScenarios["SCANcontroller"]` therefore stays at the OPERATOR-SEED BASELINE forever — per-agency state accumulates separately in `AgencyState.Coverage` + `AgencyState.Scanners`, and the projector splices it back into outbound scenario blobs at every `SendScenarioModules` tick. Under gate=off the router early-returns false and the legacy AddOrUpdate runs unchanged — dual-mode silence. **Implications when adding the next Path B slice (S3 FFT / S4 DMagic):** the router's caller-side dispatch in `RawConfigNodeInsertOrUpdate` needs ONE additional `if (scenarioModule == "<name>" && AgencyXxxRouter.TryRoute(...)) return;` line per slice; the projector's `CareerScenarios` HashSet adds the matching name; the `Project` switch adds the case. The shared D2 catch-up helper (`ScenarioSystem.SendScenariosToClient`) takes `params string[]` so the HandshakeSystem call site grows by one literal per slice. The Path B contract documented in `docs/mod-compat/implementation-spec.md` D1+D2+D3 — re-read before writing each slice.
- **The csproj Compile-list orphan check is the load-bearing first step of every mod-compat audit re-walk** (2026-05-19, session 34, S3 retirement commit `9404bfae`): the FFT (Far Future Technologies) audit was structurally invalid in a way that no field-level grep would catch — the entire `FarFutureTechnologyPersistence.cs` ScenarioModule the audit was built around exists in the upstream repo but is **NOT in `Source/FarFutureTechnologies/FarFutureTechnologies.csproj`'s `<Compile Include="..."/>` list**. The compiled FFT.dll players actually run contains no ScenarioModule. Adding the orphan back to the csproj wouldn't even compile — it references `FarFutureTechnologySettings.amFactoryConfigNodeName` (the actual settings class doesn't define this field) and the `AntimatterFactory` class (does not exist anywhere in the source tree). Stock FFT has no shared global state to partition; all state is per-PartModule already partitioned via `lmpOwningAgency`. **Recipe for the next mod audit:** before walking OnSave/OnLoad bodies, dispatch the source-walk agent to grep `<Compile Include="..."/>` from the mod's csproj FIRST and confirm the ScenarioModule file is in the list. If not, the audit's entire premise may be invalid; consider retirement before deeper analysis. Caught by the audit-via-prespec discipline running on session 34's S3 implementation prep — 30-min source walk saved ~6 hours of mis-implementation against state that doesn't exist. The `dev` branch of an upstream MIGHT compile the orphan; verify branch by branch if the operator's release version is unclear. Documented at [docs/mod-compat/near-future-and-far-future.md "Re-walked 2026-05-19"](docs/mod-compat/near-future-and-far-future.md).
- **Mod-compat S4 ships the same Path B pattern as S2 with two flattened collections + nested-wire group-by-BodyIndex on emit** (2026-05-19, session 34, commit `<S4-pending>`): DMagic Orbital Science's `DMScienceScenario` ships an asteroid-science diminishing-returns log + a per-body anomaly-records nested-list structure (`Anomaly_Records → DM_Anomaly_List → DM_Anomaly`). AgencyState stores the asteroid records flat-keyed by `Title` (matches DMagic's `recoveredDMScience` dict key) and the anomaly records flat-keyed by composite `"$bodyIndex|$name"` (storage convenience — mirrors the kolony precedent's `"$vesselId|$bodyIndex"` shape). The projector splice reconstructs the 2-level nested wire shape on emit by grouping flat anomaly entries by `BodyIndex` and producing one `DM_Anomaly_List` wrapper per body. **Verified via source walk against `F:/tmp/mks-external/DMagicOrbitalScience` SHA `a4e805b9`** ([[reference-mks-external-clones]]) — caught 5 corrections vs the 2026-05-18 audit (asteroid fields are float not double; anomaly wire is 2-level nested not flat composite; `"N5"` culture-sensitive serialization on DMagic side; `DMAnomalyObject.cs` lives in `Source/` not `Source/Scenario/`; companion event-watchers are client-side so no separate fork-side hook). No cross-agency rejection (no vessel keying); no transferagency migration. Same Path B suppression model as S2 — the SaveAgency gate-on-acceptedAny pattern and SHOULD-FIX projector per-entry Warning logging are uniform across S2/S4 to avoid disk-churn on quiet servers + ensure operability visibility. Documented at [docs/mod-compat/dmagic-orbital-science.md "Re-walked 2026-05-19"](docs/mod-compat/dmagic-orbital-science.md). `WarnAboutSharedDMagicOnUpgrade` boot diagnostic + hazard predicate join the existing diagnostic family (kolony / planetary / orbital / contracts / SCANsat) so operators flipping the gate on populated universes don't silently lose DMagic state.
- **Mod-compat S7 — pre-implementation source walk catches "audit says X but the call graph says Y" hook errors** (2026-05-19, S7 ship in `Majestic95/LunaCompat@1dc3196`): the EPL preflight audit at `docs/mod-compat/extraplanetary-launchpads.md` and the corresponding spec section both cited `ELBuildControl.BuildAndLaunchCraft` (the launch step) as the build-path prefix target and `RecyclerFSM.CollectParts` as the recycler prefix target. The multi-lens review (general/consumer/upgrade in parallel) caught BOTH as structurally wrong before ship: (a) `BuildAndLaunchCraft` runs AFTER `DoWork_Build` has drained `padResources` across many physics ticks via `padResources.TransferResource(res.name, -amount)` at `Source/BuildControl.cs:337` — the actual upfront state-flip is `BuildCraft()` at `Source/BuildControl.cs:833` which sets `state = State.Building`; hooking the launch-step would have closed the new-vessel spawn but left the pad-resource-drain griefing vector wide open. (b) `RecyclerFSM.CollectParts` is invoked from `OnTriggerStay` at `Source/Recycler/Recycler.cs:110-112` which sets `RecycleField.enabled = false` IMMEDIATELY BEFORE the call — a `CollectParts` prefix-abort would have left the trigger collider permanently disarmed because the FSM only re-enters `state_idle` (the only state with `RecycleField.enabled = true` re-set logic) via `event_parts_exhausted` from `state_captured_idle`, a chain the aborted `CollectParts` never enters. The correct recycler hook is a postfix on `ELRecycler.CanRecycle(Vessel vsl)` at `Source/Recycler/Recycler.cs:83` overriding `__result = false`, which stops `OnTriggerStay`'s `if (p != null && CanRecycle(p.vessel))` block BEFORE the disarm. **Recipe for future mod-compat preflight slices:** when audit cites a method named "X" as the right prefix target, walk the EXACT call-graph backwards from X: what state mutation begins BEFORE X is called? What state mutation already happened in earlier ticks before X is the closing step? If the answer is non-trivial, the hook is at the wrong scope. The 2026-05-19 audit was written by reading EPL's source surface; the call-graph error wasn't visible without walking the per-tick invocation order. Hook-target errors that ship silently exploit-reopen even after the operator sees "S7 armed" in the log. Mode `[[feedback-review-lens-framing]]` already covered consumer + upgrade lenses; this session adds a **call-graph lens** to the recipe: trace WHICH OTHER METHODS RUN BEFORE/AFTER your hook, not just what the hook itself does.

- **v4 VesselProto cross-agency write guard — pre-spec multi-lens review caught a load-bearing exploit the original 5.17a write-path counterpart missed** (2026-05-19, session 39, scoping doc [docs/research/v4-vessel-proto-cross-agency-write-guard.md](docs/research/v4-vessel-proto-cross-agency-write-guard.md)): the Stage 5.17a write-path counterpart from session 19 added `RejectIfCrossAgencyWrite` to 11 relayed vessel-message types + `HandleVesselRemove` + `HandleVesselCouple` — but NOT to `HandleVesselProto`. The omission was rationalised at the time via the "relayed proto bytes are advisory" framing documented in the [[5.18b relay-vs-store note]] (above). That framing was about peer-client INTERPRETATION of relayed bytes (clients re-derive ownership from VesselSync replies + AgencyVisibilityMsgData, not from relay payload). It said nothing about the server's authoritative store, which DID get overwritten by the proto bytes — the 5.16b stamp-preservation logic at `VesselDataUpdater.cs:146-160` preserves only `OwningAgencyId`; the rest of the proto payload (crew list, parts, resources, position, lmpAuthSubspace) was unprotected. **The hole was uncovered during the s39 WOLF Phase 4 pre-spec multi-lens review** when verifying §1.b's "Bob seizes Alice's kerbal via WOLF CrewRoute Launch" trace — Phase 4's `WOLF_CrewTransferScenario.Launch` calls `vessel.RemoveCrewmember(kerbal)` which feeds through `BackupVessel` → proto broadcast → ungated `HandleVesselProto`. The Phase 4 pre-spec initially asserted "5.17a + 5.17a-write-counterpart already block Bob's vessel-write"; verifying that claim by reading `VesselMsgReader.cs` revealed the proto-path gap. **Recipe:** when a write-counterpart-style guard ships against N message types, ALWAYS verify the catch-all proto/sync handlers were also covered — they typically receive different handler bodies (not the per-type switch) and get omitted from "applies to all relayed types" coverage claims. **Symptoms of the gap:** any modified client crafting a `VesselProtoMsgData` for another agency's vessel-id could mutate that vessel's state on the server-canonical disk + force the mutation to broadcast to all peers — far broader than the WOLF-specific kerbal-seizure path. Fix is a 1-line addition of `if (RejectIfCrossAgencyWrite(...)) return;` in `HandleVesselProto` between the past-subspace check and the sender-agency resolution. Bypass cases (gate off / vessel not in store / Empty-sentinel / sender no agency) mirror the existing helper's contract; vessel-not-in-store is the CORRECT default for proto path since proto IS the legitimate entry point for new vessels and the 5.16b stamp logic auto-routes them to the sender's own agency. **Documented limitation:** race-craft-pre-create — Bob could pre-broadcast a proto for a vessel-id Alice is about to create, locking her out via the vessel-not-in-store bypass. Narrow attack (requires side-channel knowledge of Alice's freshly-minted Guid + ~2.5s race window); deferred to Stage 6 / `VesselReserveMsgData` pre-registration design. v4 protocol stays at 0.31.0 — no wire change. Dual-mode silence preserved (gate off → guard returns false immediately). Phase 4's `§8.e Path A re-derivation` had to be corrected in the same session — it had been built on the assumption that the proto path was already gated.
- **Path B sequencing for v4/v5 — small focused fixes ship first to ground larger feature work** (2026-05-19, session 39): when a pre-spec multi-lens review uncovers a load-bearing exploit (here: the proto-write hole above), the established sequencing is to ship the FOCUSED fix in the current release line (v4 = proto-guard, 1 commit, ~half-day) BEFORE the FEATURE release line (v5 = WOLF Phase 4, ~2-3 weeks, 5-6 commits) ships features that lean on the fix being in place. Rationale: (a) the feature pre-spec's correctness claims are grounded on the fix's behaviour; deferring the fix would force documentation rework saying "this exploit is still open until v6"; (b) the fix is so small relative to the feature (1-3% of effort) that the upfront cost is negligible; (c) release notes are cleaner when v4 = "stability + security hardening" and v5 = "WOLF per-agency feature" instead of v4 = "feature" + v5 = "feature + retroactive fix". The trade-off is a half-day delay before WOLF Slice A starts; the alternative (defer proto-guard, document the residual exploit, ship Phase 4) is acceptable only if the cohort is trusted (small private playtesters who don't run modified clients) and the operator explicitly accepts the residual exploit window. **Pattern for future cycles:** any time a pre-spec lens review uncovers a related-but-separable exploit during feature design, default to the "fix first, feature after" sequencing unless the operator explicitly opts otherwise.

- **WOLF Phase 4 — 6-slice arc closing Phase 4 with kerbal-restoration cascade as the load-bearing Slice F surface** (2026-05-19, sessions 38-42, branch tip post-Slice F): Phase 4 partitioned MKS' Wireless Orbital Logistics Function (WOLF) into per-agency state across 5 dicts on `AgencyState` (`WolfDepots` / `WolfRoutes` / `WolfHoppers` / `WolfTerminals` / `WolfCrewRoutes`). The slice topology was driven by FK structure + the entity-type quirks: Slice A wire-only foundation (5 entity classes + 5 MsgData + enum slots 9-13; symmetric `SubTypeDictionary` appends; ConfigNode round-trip with forward-compat); Slice B server-side depot routing foundation; Slice B-2 atomic-activation of the projector splice + 3 player-driven postfixes + hazard predicate (couldn't ship in B alone — partial activation would have left gate=on operators with empty per-agency WOLF UI); Slice B-3 Negotiate-debounce hot path (coalesces ~10/s click-driven mutations into 250ms-tail debounced sends); Slice C Routes router + FK-swept projector (introduced the `depotKeySet` HashSet pattern); Slice D Hoppers + Terminals routers (Hoppers FK-coupled to depots; Terminals no-FK per WOLF source contract); Slice E CrewRoutes + **the distinctive Phase 4 surface — cross-agency kerbal authority gate via vessel-proxy K1-pattern scan** (mirrors `KerbalSystem.CanRemoveKerbalUnderK1` from 5.17e-8; `BuildKerbalAgencyMap` snapshots `OwningAgencyId` once per vessel + applies the [[5.18b relay-vs-store note]] non-Empty-preserves-over-Empty rule against the launder-via-Empty grief vector; integration-logic lens caught the pre-spec missing `CheckArrived` auto-transition postfix → 5th Harmony patch using `__state` pattern); Slice F server-only migration helpers — `AgencyWolfMigration.CascadeOnDelete` restores in-flight CrewRoute passengers from `RosterStatus.Missing` + `SetTimeForRespawn(double.MaxValue)` back to AC-pool `state = Available` + `ToD = 0` via depth-aware kerbal-file rewrite, invoked from `DeleteAgencyCommand` BEFORE `TryDeleteAgency` removes the agency record (the WolfCrewRoutes dict is no longer accessible after registry removal). Two non-obvious Slice F design decisions: (1) **restoration scope is {Enroute, Arrived} only, NOT {Boarding, Enroute}** — verified against `F:/tmp/mks-external/MKS/Source/WOLF/WOLF/Modules/WOLF_CrewTransferScenario.cs:586-590` + `:155`; `rosterStatus = Missing` happens only at Launch (Boarding→Enroute transition); Boarding passengers are still Assigned on their source vessel; Arrived passengers stay Missing until operator clicks Disembark (`CheckArrived` mutates `FlightStatus` only); restoring Boarding would gratuitously stomp Assigned→Available. The s42 pickup memo's `{Boarding, Enroute}` framing was incorrect — caught by source re-walk per [[feedback-research-first]]. (2) **No `.Clear()` of the 5 WOLF dicts** — `TryDeleteAgency`'s precedent is to remove the AgencyState from `Agencies` + delete the on-disk file + GC the lock + evict claims; it does NOT clear KolonyEntries / OrbitalTransfers / Contracts / etc., which all vanish naturally with the unreachable AgencyState reference. The 5 WOLF dicts get the same treatment. **`/transferagency` and `/setvesselagency` are NO-OPs for WOLF** because WOLF entities are body+biome / Guid keyed, not vessel-keyed; the in-Enroute kerbal corollary (a kerbal currently in an Enroute CrewRoute is NOT moved by `/setvesselagency` on their carrier vessel — passenger list is fixed at `CreateCrewRoute` time per WOLF contract) is documented in the command XML to prevent Slice G+ maintainers from adding redundant migration walks. **EnsureDepotKeySet extraction lesson**: Slice C had one consumer of `depotKeySet`; Slice D added a second (duplicated inline); Slice E was the third → time to lift to a local function shared by Routes/Hoppers/CrewRoutes. The "3rd consumer = extract" rule is now established for WOLF projector helpers. Race-window posture across all 6 slices: the cascade snapshot/restoration phase has a microseconds-narrow window between cascade-lock-release and TryDeleteAgency's lock-acquire where a router could accept a new Launch; mitigated by existing operator-/kick-first workflow advice at `DeleteAgencyCommand.cs:226+`. Documented + accepted; structural closure would require holding the agency lock across multi-disk-IO restoration which violates the established lock-domain split. Tests: ServerTest 600 → 663 across all 6 slices (+30 D + +26 E + +7 F = ServerTest, plus MockClientTest +6 e2e in Slice D). Protocol stays at 0.31.0 throughout — additive enum slots only.

- **Per-agency auto-unlocked tech nodes need explicit server-side seeding — `AgencyTechRouter` only sees state mutations that go through the wire path, but KSP has paths that bypass the wire entirely** (2026-05-20, v6 soak): the `start` Tech node in stock Career mode is auto-unlocked at game-creation time by KSP's `Game.CrewAssignmentDialog` flow which calls `ResearchAndDevelopment.Instance.UnlockProtoTechNode` directly — NOT `ResearchAndDevelopment.UnlockTechWithParts`, which is the only path that fires `ShareProgressTechnologyMsgData`. Per-agency mode therefore never saw the `start` unlock; `AgencyState.TechNodes` was permanently missing the entry; the projector at `AgencyScenarioProjector.cs:750-751` stripped the shared `start` node from outgoing R&D scenarios on every send and re-spliced nothing in its place. Players saw "Unavailable Experimental Parts" on any vessel using basic stock parts (mk1pod / parachuteSingle / basicFin / etc.). Fix: `AgencySystem.EnsureStartTechSeeded(state)` helper reads canonical `start` Tech bytes from `ScenarioStoreSystem.CurrentScenarios["ResearchAndDevelopment"]` (under the per-scenario semaphore — BUG-033 contract), inserts under per-agency lock into both `TechNodes["start"]` AND `PurchasedParts["start"]` (both needed: Tech bytes carry `part = X` lines that the projector splices verbatim, but KSP treats those as "part lives here" not "purchased" — universes with non-zero `entryCost` on Start's parts would still lock players out). Called from both `RegisterAgency` (mint) AND `LoadAgencyFromFile` (backfill — pre-fix agencies get fixed transparently on next server restart). Idempotent `ContainsKey("start")` guard; helper returns bool so callers do a single conditional `SaveAgency`. Career-only via existing `PerAgencyEnabled` gate. **Broader pattern this is a symptom of:** the per-agency router design assumed all state mutations flow through wire paths it intercepts — but KSP has multiple paths for the same operation. Future per-agency routers MUST enumerate ALL upstream KSP code paths that mutate the target state and either intercept each one OR explicitly document why the path doesn't apply (and write a smoke test confirming the assumption holds). See `docs/research/09-post-incident-systemic-improvements.md` for the ranked backlog of structural prevention measures (smoke test #1, boot-time invariant audit #2, client-error telemetry #3). Tests: ServerTest 663 → 670 (+7 in `AgencyStartTechSeedingTest`), MockClientTest +1 e2e in `AgencyStartTechProjectionTest`.

- **Stage 6 pre-implementation gap pass + external-seat 2018 deletion + EVA dual-channel wire flow** (2026-05-20, Stage 6 Phases 6.1 + 6.2, commits `64d5f2e1` + `c319d475`; Gap 5 separable bugfix `d500238c`): Stage 6 (per-agency kerbal roster) launched with a **pre-implementation gap pass** that cross-referenced the spec against actual source. Four parallel kerbal-surface audits (core / mod-compat / vessel-proto / KSP event taxonomy) surfaced 5 gaps. Four were spec amendments folded into the §0 + §3 + §Q-K1 + §Q-CrossAgency + §9 amendments shipped in 6.1; the fifth was a pre-existing client-side bug — `LmpClient/Harmony/KerbalEVA_BoardSeat.cs` + `KerbalEVA_OnDeboardSeat.cs` Harmony patches raise `ExternalSeatEvent.onExternalSeatBoard / onExternalSeatUnboard` but no subscriber existed in `LmpClient/` since 2018 commit `b7306514` (the docking refactor that introduced `VesselCoupleSys` deleted Dagger's `ExternalSeatSystem` + `ExternalSeatEvents` as collateral — VesselCoupleSys covers vessel-vessel docking, NOT kerbal-into-seat boarding; different KSP code paths). Result: when a kerbal sat in a lawn-chair external seat, peers never saw it; the seat stayed empty across the network until the next periodic vessel resync (if any). Fixed by re-subscribing in `VesselCrewSystem` (the existing unified crew-lifecycle home) with handlers mirroring the modernized `OnCrewBoard` shape — board sends VesselRemove for the EVA-vessel + releases locks + KillVessel + VesselProto for the seat-owner; unboard sends VesselProto for the seat-owner + `EvaReady.FireOnCrewEvaReady` for the fresh EVA-vessel because `OnDeboardSeat` does NOT reliably fire `onCrewOnEva` (that event is for IVA→EVA out of internal seats, not external-seat unboard).

  **Key Stage 6 wire-flow lesson documented in spec §3:** EVA / boarding crew mutations route across **two channels** and Stage 6 only routes one. Channel 1 is `KerbalProtoMsgData` (fired by `KerbalEvents.StatusChange` on Active↔Eva status change — the per-kerbal-file mutation Stage 6 partitions per-agency). Channel 2 is `VesselProtoMsgData` (fired by `VesselCrewEvents.OnCrewBoard / OnExternalSeatBoard / CrewEvaReady` carrying the updated `protoCrew` list on the seat-owner vessel). The v4 `RejectIfCrossAgencyWrite` at `Server/Message/VesselMsgReader.cs:HandleVesselProto` (session 39 fix) already gates channel 2 — Stage 6 implementers must NOT add a second-layer guard there. Channel 1 is the gap Stage 6 closes; channel 2 is already safe.

  **Pre-implementation gap pass methodology:** before writing any Phase 6.2 code, four parallel Explore agents read the actual source for (a) every `LmpClient/Systems/KerbalSys/` + `LmpClient/Harmony/*Kerbal*` file, (b) every mod-compat router (MKS Phase 3 + WOLF Phase 4 Slices A-F + S1/S2/S4), (c) every scenario-projector splice for indirect kerbal-name references, (d) every KSP GameEvent subscription that fires kerbal-state changes. The audit caught: (i) Tourism/Rescue contracts are REJECTED at offer time in `ShareContractsEvents.cs:545-557` so the spec's per-agency-tourist plan was unreachable (moved to §9 Out of Scope); (ii) the dual-channel wire flow above; (iii) `ProtoCrewMember_Die.cs` Harmony patch is a K1 sibling structurally moot under gate=on; (iv) per-agency kerbal writes must upgrade from `WriteToFile` to `WriteAtomic` because each per-agency kerbal file is the ONLY copy (gate=off keeps `WriteToFile` because shared defaults are regenerable); (v) the external-seat 2018 deletion. **The cost of the audit was ~30 minutes of parallel agent work; without it, the Phase 6.4/6.5 implementer would have written tourist-handling code that never runs, missed the channel-2 safety story, and inherited the seat-sync bug as a "Stage 6 broke external seats" false attribution.** Recipe: any future Stage-N spec that depends on existing client/server code paths should run the parallel-Explore-agent audit before phase 1 ships, then spot-read at least 3 load-bearing files per `[[feedback-research-first]]` to validate the audit summary. Phase 6.2 ServerTest 671 → 682 (+11 in `PerAgencyKerbalRosterScaffoldingTest`); LmpClientTest 91 → 165 unaffected by Gap 5 fix (handlers depend on static singletons; soak validation only).

- **Stage 6 Phase 6.3 — per-agency kerbal disk layout + Phase 6.5 race-window forward-warning + lazy-FolderCreate shared-lock invariant** (2026-05-20, commit `90470f08`): Stage 6 Phase 6.3 ships the disk-layout foundation for per-agency kerbal rosters. New helper `AgencySystem.GetKerbalsPathForAgency(Guid)` resolves to `Universe/Agencies/{guid:N}/Kerbals/` — sibling of the `{guid:N}.txt` agency state file under the same `AgencyState.AgenciesPath`. Folder + file with same stem coexist cleanly on NTFS/ext4/APFS (different inode namespaces); legal but unusual disk layout, worth knowing for any future tooling that walks `Universe/Agencies/`. **Gate choice** is `PerAgencyEnabled` (PerAgencyCareer + Career mode) NOT the combined `PerAgencyKerbalRosterEnabled` — disk layout is laid down up-front for every per-agency-career universe, so a mid-cohort flip of `PerAgencyKerbalRoster=true` needs no operator-facing backfill pass. The ~4kB-per-agency cost of seeded-but-unused stock kerbal files is negligible relative to the operational simplification. **Lazy-FolderCreate-with-shared-lock invariant** (persistence-lens review s44): `FileHandler.WriteAtomic` does NOT auto-create parent directories (wraps `File.WriteAllText` which throws `DirectoryNotFoundException`). `SeedStockKerbalsForAgency` therefore calls `FolderCreate(kerbalsDir)` lazily on the FIRST iteration that actually writes — all-4-present idempotent re-call does zero IO. The `FolderCreate(kerbalsDir)` and the subsequent `WriteAtomic(path, content)` calls all serialize on the SAME `FileHandler.GetLockSemaphore` lock anchor because `Path.HasExtension(kerbalsDir)` is false → key=path-itself, while `Path.HasExtension(.../Kerbals/X.txt)` is true → key=`Path.GetDirectoryName(...)` which also resolves to `.../Kerbals`. Future optimisers must NOT hoist `FolderCreate` to a different lock anchor (e.g. the parent agency-folder lock) without breaking the serialization. Code comment in `SeedStockKerbalsForAgency` warns about this. **Phase 6.5 race-window** (persistence-lens S2): `TryDeleteAgency` cascades `FolderDeleteRecursive(GetKerbalsPathForAgency(agencyId))` AFTER `Agencies.TryRemove` + `AgencyByPlayerName.TryRemove` but INSIDE the same per-agency lock scope. Phase 6.5's `HandleKerbalProto` write path will use `FileHandler.WriteAtomic` and will NOT hold the per-agency lock — so a KerbalProto arriving microseconds after the registry-remove but before the cascade-delete could create a new file inside a subdir about to be recursively deleted, silently losing the file. When Phase 6.5 ships, the handler MUST: (a) resolve sender→agencyId, (b) acquire `GetAgencyLock(senderAgencyId)`, (c) re-check `Agencies.ContainsKey(senderAgencyId)` under that lock, (d) bail if the agency was just deleted. Without the guard, the cascade race produces orphan files that next `LoadExistingAgencies` skips (no `.txt` for the dead id) but waste disk + confuse operator forensics. **Sibling-subdir lock-anchor difference** (server-systems-lens C3): `FolderDeleteRecursive` on `…/Kerbals` anchors on the `…/Kerbals` lock; the empty-parent cleanup `FolderDelete` on `…/{guid:N}` anchors on the DIFFERENT `…/{guid:N}` lock. A future Stage-6+ sibling subdir under `{guid:N}/` (per-agency Achievements / Flags / etc.) would be serialised against Kerbals writes (same `Kerbals/` lock-anchor) but NOT against the empty-parent cleanup — needs its own `FolderExists`+empty-cleanup branch + matching write-time lock-acquisition discipline. Both warnings shipped in code comments in `TryDeleteAgency`'s cascade block. Tests at `ServerTest/PerAgencyKerbalDiskLayoutTest.cs` (+11 → 693 baseline).

- **Stage 6 Phase 6.4 — half-shipped-state hazard + temporary boot-refusal pattern + LINQ-deferred-read catch widening** (2026-05-20, this commit): Phase 6.4 ships the FIRST per-agency kerbal handler — `HandleKerbalsRequest` resolves the source directory via the new `KerbalSystem.ResolveKerbalsPathForRequester(string playerName)` internal pure helper (4 branches: dual-mode-silence; defensive `AgencyByPlayerName`-miss fallback; happy-path agency subdir; subdir-absent fallback). Two non-obvious lessons from the multi-lens review: **(1) half-shipped-state hazard requires a temporary boot-refusal predicate.** Phase 6.4 alone routes reads per-agency but `HandleKerbalProto` + `HandleKerbalRemove` (Phase 6.5 territory) still pool writes to `Universe/Kerbals/`. The Phase 6.2 boot-refusal (`RefuseStartupIfKerbalHazardWithoutOverride`) only fires when legacy `Universe/Kerbals/` is populated AT BOOT — fresh-mint universes pass it cleanly, then accumulate session writes to legacy that the per-agency request handler never reads back. Result: silent state divergence on every Phase-6.4-only universe with gate=on. Fix is a NEW predicate `AgencySystem.RefuseStartupIfKerbalWriteRoutingNotYetShipped` that fires unconditionally under `PerAgencyKerbalRoster=true` regardless of universe state or override flags. The predicate is REMOVED in the Phase 6.5 commit; two Phase 6.2 scaffolding tests that previously asserted "boot allowed under override" are temporarily flipped to assert refusal (look for `_UntilPhase65Ships` test-name suffixes) and must flip back when 6.5 lands. **Recipe for future multi-phase-handler-routing landings:** whenever a read-path and write-path counterpart land in separate commits, ship a temporary unconditional refusal between the two so an operator opting in early gets an explicit refusal instead of silent corruption. The refusal cost is ~30 lines including the doc comment + one test pinning its behaviour; removal is a 4-line delete in the follow-up commit. Catches the kind of failure where boot-refusal predicates designed for upgrade-in-place don't cover fresh-mint mid-session divergence. **(2) `DirectoryNotFoundException` alone is the wrong catch for LINQ-deferred enumerations.** The initial Phase 6.4 draft wrapped only `FileHandler.GetFilesInPath(sourcePath)` in `try { } catch (DirectoryNotFoundException)`, materialised `kerbalsData` as a deferred LINQ `Select(k => ReadFile(k))`, then called `.ToArray()` in the `KerbalReplyMsgData.Kerbals = ...` assignment OUTSIDE the catch. A `/deleteagency` cascade interleaving between `Directory.GetFiles` return and per-file `ReadFile` execution would throw `FileNotFoundException` from inside the deferred Select, escape to the receive thread, and NRE-kill the listener — the exact failure mode the catch was supposed to prevent. **Two changes required:** widen the catch to `IOException` (parent class for `DirectoryNotFoundException` / `FileNotFoundException` / `UnauthorizedAccessException`'s IOException base, covering disk-vanish + permissions-flip uniformly); AND materialise the full pipeline INSIDE the try via `.ToArray()` BEFORE the catch closes, so the deferred Select can't outlive the protection. General pattern: when wrapping LINQ that includes I/O calls in a try/catch, always materialise inside the try, and pick the exception type one level up the hierarchy from the obvious one. Phase 6.4 doc comment in `HandleKerbalsRequest` body warns future maintainers. **(3) Spec text correction — operators on populated upgrades.** The original §Q-Migration text said legacy kerbals "remain readable" post-opt-in, which was true in the literal disk-file sense but misleading about player-facing UX (the per-agency request handler never reads legacy under gate=on — players see only the stock-4 seed regardless of what's on disk). Spec corrected to "inaccessible to clients" with explicit operator hand-copy recovery path. Lesson: when documenting upgrade behaviour, write the SENTENCE the operator would WANT to read (i.e. "what does the player see") rather than the literal mechanical truth ("the file exists on disk"). The two framings can diverge.

- **Stage 6 Phase 6.5 — write-path routing + cascade-race guard + four review lessons** (2026-05-20, this commit): Phase 6.5 closes the write-path counterpart to Phase 6.4's read filter. New internal pure helpers `KerbalSystem.TryWriteKerbalProtoPerAgency` + `TryDeleteKerbalPerAgency` resolve sender→agency, acquire `GetAgencyLock`, re-check `Agencies.ContainsKey` under the lock to close the cascade race documented in Phase 6.3's commit, then write via the new `FileHandler.WriteAtomic(byte[], int)` overload / delete via `FileHandler.FileDelete`. Caller relays via `RelayToSameAgencyClients<T>` — under 1:1 OwningPlayerName design selects zero peers, forward-compat with multi-player-per-agency. Phase 6.4 TEMPORARY boot-refusal predicate REMOVED; the combined gate is now fully functional. **Four lessons documented in the review-driven changes:** **(1) ContentsAreEqual short-circuit needed for chatty atomic writes.** The initial `WriteAtomic(byte[], int)` draft unconditionally rotated + rewrote on every call. KerbalProto fires on every periodic vessel-resync (every ~10s, every kerbal); the rotation would clobber `.bak` to "previous identical generation" within a single tick, shrinking the crash-recovery window to nothing. Symmetric with `WriteToFile(byte[], int)`'s pre-existing `ContentChecker.ContentsAreEqual` guard. **General pattern:** when adding an atomic-write overload to a hot path, mirror the legacy non-atomic overload's no-op short-circuit too — even though the API contract doesn't require it, the operator-facing crash-recovery contract DOES. **(2) DROP-on-mapping-miss must be asymmetric between read and write under per-agency mode.** Phase 6.4 read-side falls back to legacy on `AgencyByPlayerName` miss; Phase 6.5 write-side DROPS instead. Falling back to legacy on the write side under gate=on would silently land mutations in a directory the request handler never reads back — the gate=on equivalent of `/dev/null`. The DROP makes the data loss audible (operator sees the Warning); the fallback would make it silent. Documented in the helper XML. **General pattern:** dual-mode-silence asymmetries between read and write paths are often correct — the failure modes are different (read returns potentially-stale data; write disappears). **(3) Cascade race needs same-lock re-check pattern.** `TryDeleteAgency` holds `GetAgencyLock(agencyId)` across `Agencies.TryRemove` + `FolderDeleteRecursive`. Without the matching `lock + ContainsKey re-check` in HandleKerbalProto, a KerbalProto arriving microseconds after `TryRemove` but before the recursive folder delete would create a file inside the about-to-be-deleted subdir, silently lost. The pattern: any code that writes inside a per-agency subdir under gate=on must acquire the same per-agency lock and re-check Agencies-membership inside it. Documented in Phase 6.3's commit, applied here. **General pattern:** when a cleanup-cascade holds a per-key lock, every concurrent writer that targets the same key MUST acquire-then-re-check inside the lock. **(4) Negative gate-on assertions need positive parity proofs.** Per `[[feedback-negative-assertions-lock-in-bugs]]` — the Phase 6.5 relay test originally only asserted "Bob does not receive Alice's KerbalProto" under the current 1:1 design. The `RelayToSameAgencyClients` filter is supposed to forward-compat to future multi-player-per-agency, but without a POSITIVE test pinning "a same-agency peer DOES receive the relay," a refactor could silently break the filter's positive-match path and the gate-on negative tests would not catch it. Fix: synthesise the future state by reassigning `AgencyByPlayerName[playerB] = aliceAgency` in a dedicated test, send Alice's proto, assert Bob receives it. Same pattern caught the contract-relay regression in v1→v2→v3 (per the memory entry). **General pattern:** every "X must not happen" gate-on test needs a paired "X DOES happen when the gate state changes appropriately" test against the same machinery. **Known limitation deferred to Phase 6.7:** `AgencyWolfMigration.CascadeOnDelete` still writes restored kerbal files to legacy regardless of gate. Under Phase 6.5 + gate=on, restored kerbals are unreachable to clients of any surviving agency. Phase 6.5 surfaces this via an audible Warning in CascadeOnDelete when gate=on + in-flight CrewRoutes; Phase 6.7 reworks the routing. The mitigation pattern (warn + defer + manual-recovery instructions) is acceptable when the underlying fix involves a design decision that's beyond the current commit's scope — but the warning must include manual-recovery steps so an operator hitting the limitation has a path forward, not just a "this might be broken" line.

_Append new entries chronologically. If a note becomes obsolete, prefer striking it through with a date rather than deleting outright, so future-you sees the lesson._

---

## Known Limitations & Future Work

- **Logging:** ring buffer + tagged overloads shipped Stage 1.2. Size-based rotation deferred (daily + expire suffice until dashboard ships); `LogSettings.RingBufferSize` setting deferred (`LogRingBuffer.Capacity` is a `const`).
- **Admin dashboard:** Stage 3.7 v1 shipped — `GET /fork` exposes `ForkInformation` (fork name + protocol version + `ActiveFixes[]`); `GET /log` exposes a `LogRingBuffer` snapshot. Both are JSON, both via the new `JsonGetHandler`. v1 is read-only; no HTML, no auth, no filtering. v2 might add level/subsystem query filters and a tiny HTML view — defer until an operator actually asks.
- **Mock-client test harness:** Stages 4.9 + 4.10 + 4.11 all shipped (sessions 4-9). `MockClientTest` brings the real Server up in-process on a free UDP port; `MockNetClient` completes the LMP handshake against it. Server-observable regressions covered: BUG-001 / BUG-005/006 / BUG-051a / BUG-010 Part A / BUG-025. Client-internal decision-math regressions covered in `LmpClientTest/`: BUG-003/004 + BUG-051b + BUG-008 Phase A. CI runs the four server-side suites on every PR + every push to master; LmpClientTest is local-only by design (needs KSP DLLs).
- **Per-agency career:** Stage 5, not started. Lives on `feature/per-agency` branch (also not yet created).
- **Pre-existing build warnings (30):** noise to be tackled in a dedicated pass, not piecemeal.
- **`GroupSystem` is scaffolding:** name + member list only, no resource fields. Needed for per-agency.
- **Bug inventory:** see `docs/research/01-bug-inventory.md` for the full 52-bug catalogue (BUG-051 + BUG-052 added during Phase-2). All ten top-10 bugs are now closed: BUG-001 / BUG-005 / BUG-008 Phase A / BUG-010 / BUG-013 / BUG-018 / BUG-023 / BUG-025 / BUG-033 / BUG-045. Eight of those landed on this fork; two (BUG-018 + BUG-019/024 cluster) adopted from upstream PR #687. Phase B of BUG-008 (server-side stored `terrainAltitude`) is the only top-10-adjacent deferred item; revisit once Phase A soaks in real play. Future Phase-2 / fix work picks up from the longer bug list at this point — no remaining top-10 backlog.
- **BUG-001 rejoin race:** documented known limitation. Solo→non-solo transition may fire one snap because server's `Subspaces[id].Time` is stale relative to the solo player's UT. Follow-up: have the client report its UT delta on rejoin so server can refresh before broadcasting.
- **Couple handoff covers dock only:** `HandleVesselCouple` sets the merged vessel's `AuthoritativeSubspaceId` to the initiator's. Undock relies on the new child vessel's first proto-update to stamp authority via the standard rule — adequate for typical KSP flows but not as explicit as the dock path.

---

## Stage Roadmap

Master plan (also tracked in conversation todos for active work):

- **Stage 1 — Foundations** ✅ COMPLETE
  - ✅ 1.1 Backup archives + restore (`f4aed253`)
  - ✅ 1.2 Logging upgrade — ring buffer + tagged overloads + parse-back capture (`4e3e6bd2`)
  - ⏸ 1.3 Upstream coordination meta-issue — **DEFERRED INDEFINITELY** per fork-master strategy
- **Stage 2 — First visible stability win** ✅ COMPLETE (2026-05-16)
  - ✅ 2.4 Phase-2 analyses for time/subspace bugs (`48df64bd`, `fc2b793a`)
  - ✅ 2.5a BUG-051a server-side request dedup (`9732fc7e`)
  - ✅ 2.5b BUG-001 solo-subspace catch-up (`0f10b2d3`)
  - ✅ 2.5c BUG-003/004 future-subspace interpolation cap (`cd551859`)
  - ✅ 2.5d BUG-051b client steady-state retry (`25303e7d`)
  - ✅ 2.5e BUG-014 audit-closed (`7f1393f4`)
  - ✅ 2.5f BUG-005/006 cross-subspace lock keying + protocol bump 0.30.0 (`d64acf66`)
  - ✅ 2.5g Fork-build banner + `[fix:BUG-XXX]` log tags (`d2186e2e`)
  - ⏸ 2.6 First upstream PR — **DEFERRED** by strategy shift
- **Stage 3 — Operational tooling (3–4 weeks, parallel)** — ✅ COMPLETE (2026-05-17, session 9)
  - ✅ 3.7 Admin dashboard v1 — `/fork` + `/log` (text/plain, human-readable) + `/logjson` (tooling) endpoints. Backed by `ForkBuildInfo` + `LogRingBuffer`. v2 (level/subsystem filters, HTML view, basic auth) deferred.
  - ✅ 3.8 Phase-2 + fixes for remaining top-10 — ALL 10 TOP-10 BUGS CLOSED. BUG-013 (`c5ab8fa5`), BUG-008 Phase A (session 6, `PqsAlignmentRoutine` + `VesselProtoSystem` hook — Phase B deferred until Phase A soaks), BUG-018 / BUG-019 / BUG-024 (upstream PR #687 audit), BUG-045 (Strategy B.1), BUG-010 Parts A+B (session 7), BUG-033 (session 8, `87105f41` — `BackupScenarios` + `GetScenarioInConfigNodeFormat` under per-scenario writer lock), BUG-025 (session 9, `83905d4d` — server check-and-claim + rejection message + ShareScienceSystem-bracketed refund), BUG-023 (session 9, `5a240c32` — three-part port from Drew Banyai's Release/0_29_2: load-time `ScrubInvalidProtoCrew` + `KerbalsToProcess` drain + Harmony defense-in-depth). See `docs/research/01-bug-inventory.md` for the full closure ledger.
- **Stage 4 — Mock-client test harness (2–4 weeks)** — ✅ COMPLETE (2026-05-17, session 9)
  - ✅ 4.9 Protocol harness v1 — `MockClientTest` project with `ServerHarness` (real Server in-process on a free localhost UDP port) + `MockNetClient` (Lidgren peer speaking the LMP wire protocol via the production factories). `HandshakeSmokeTest` proves the wiring end-to-end. Design at `docs/research/04-mock-client-harness-design.md`.
  - ✅ 4.10 Regression tests for shipped fixes — server-observable bugs covered by `MockClientTest`: BUG-051a (`Bug051aDedupTest`), BUG-001 (`Bug001SoloBroadcastTest`), BUG-005/006 (`Bug005SubspaceRejectTest`), BUG-010 Part A (`Bug010PinnedBroadcastTest`), BUG-025 (`Bug025RejectionTest`). Client-internal decision math covered by `LmpClientTest` (net472): BUG-008 Phase A (`PqsAlignmentDecisionTest`, session 6), BUG-003/004 (`InterpolationCapDecisionTest`, session 9 — `VesselPositionUpdate.ComputeMaxInterpolationDuration` extracted as a pure helper), BUG-051b (`SteadyStateRetryDecisionTest`, session 9 — `WarpSystem.ShouldSteadyStateRetry` extracted as a pure helper). Also surfaced + fixed [BUG-052](docs/research/01-bug-inventory.md) (vendored Lidgren `NetReliableSenderChannel.DestoreMessage` NRE on late-ACK during peer shutdown) while writing the first MockClient test.
  - ✅ 4.11 CI integration — `.github/workflows/build.yml` (GHA Ubuntu, `actions/setup-dotnet@v4` pinned to 10.0.x) runs build + test for `Server` / `ServerTest`, `MasterServer` / `LmpMasterServerTest`, `LmpCommonTest`, and `MockClientTest` on every PR AND every push to `master`. `LmpClient` + `LmpClientTest` are intentionally NOT in CI — they require `External/KSPLibraries/` (KSP-licensed Unity/KSP DLLs) which cannot be checked into the repo. Run those locally with the user-installed .NET 10 SDK + KSP DLL drop before pushing. Solving the KSP-libs-in-CI problem (LFS, stripped DLL set, secret-protected archive) is a separate Stage-4.12+ decision and not blocking closure.
- **Stage 5 — Per-agency career (2–4 months, separate branch)** — IN PROGRESS on `feature/per-agency`. Spec at [docs/research/05-per-agency-spec.md](docs/research/05-per-agency-spec.md). Progress tracker at [docs/research/05a-stage5-progress.md](docs/research/05a-stage5-progress.md).
  - ✅ 5.12 Create `feature/per-agency` (2026-05-17 session 10, branched from `master` at `6515e006`). Q1-Q4 + migration/CommNet defaults signed off and recorded in spec §10.
  - ✅ 5.13 Audit PlagueNZ — [docs/research/05a-plaguenz-audit.md](docs/research/05a-plaguenz-audit.md). Benchmark only, no code adopted.
  - ✅ 5.13b Q5/Q6/Q7 audit-driven design checks resolved + spec §2/§3/§5/§6/§10/§12 amended (session 11, `51cd86d7`).
  - ✅ 5.14a `PerAgencyCareer` setting (default false) + protocol bump 0.30.0 → 0.31.0 + ForkBuildInfo entry (session 12, `49583ec5`).
  - ✅ 5.14c `FileHandler.WriteAtomic` / `ReadAtomic` + `Server/System/Agency/AgencyState.cs` ConfigNode round-trip + `Universe.CheckUniverse` hookup (session 12, `d0b484c4`).
  - ✅ 5.15a `Server/System/Agency/AgencySystem.cs` lifecycle (register/load/save/boot-load) + `HandshakeSystem` hook + heal-on-bak-recovery (session 12, `f45cd891`).
  - ✅ 5.15b Wire protocol — registration set (4 messages: Handshake / CreateRequest / CreateReply / State) + `AgencySrvMsg` ch 22 / `AgencyCliMsg` ch 21 + enum entries (session 12, `b1644532`). Scope re-derived from spec §4: 9 mutation+visibility messages deferred to consumer steps.
  - ✅ 5.15c Server-side handlers (`Server/Message/AgencyMsgReader.cs`) + outbound sender (`Server/System/Agency/AgencySystemSender.cs`) + `HandshakeSystem` extension to push Handshake+State on auth (session 12, `57bbc2b1`). Privacy rule (spec §10 Q1): `AgencyStateMsgData` is owner-only; cross-agency goes through public `AgencyInfo` summary.
  - ✅ 5.16a MockClientTest agency harness (session 13, `d9161c27`). Five end-to-end tests in `MockClientTest/AgencyHandshakeTest.cs` against the in-process server; harness extended with `Universe.CheckUniverse` at Start + `AgencySystem.Reset`/`PerAgencyCareer=false` in per-test reset + `MockNetClient` inbox rework (List+ManualResetEventSlim replaces destructive BlockingCollection so cross-channel arrival order doesn't drop messages — first motivating case was Stage 5.16a's HandshakeReply ch 1 vs AgencyHandshake ch 22, but the property is general). MockClientTest 17/17 (was 12).
  - ✅ 5.16b `lmpOwningAgency` on vessels (session 13, initial `076d3486` + 5 review rounds `f0129746` `e4b8f781` `3973f77f` `ebdee7ae` `5499101d`). Server stamps proto sender's agency on first-sight vessels; preserves existing OwningAgencyId (including `Guid.Empty` = Unassigned sentinel per spec §10 Q3) on subsequent protos; dual-mode silence gates the entire stamp block on PerAgencyCareer. 5 review rounds (general deep / server+net parallel / server+net+persistence parallel / convergence / consumer+upgrade-lens parallel) found 7 real production bugs the prior rounds missed — see "Stack Notes" entry below on lens-framing. ServerTest 140/140 (was 131), MockClientTest 23/23 (was 17). Stage 5.16 is shippable.
  - ✅ 5.17a `LockSystem` cross-agency rejection (session 13, commit `3fb85379`). `AcquireLock` refuses vessel-scoped lock acquires when the requester's agency does not match the vessel's `OwningAgencyId`. Bypass cases: gate off, non-vessel-scoped lock type, empty VesselId, Unassigned-sentinel vessel (any agency may interact per spec §10 Q3), requester has no agency mapping. **Reject under gate=on when vessel not in CurrentVessels** — closes the proto-ingest-vs-lock-acquire race (Task.Run stamp + synchronous relay would otherwise let a peer claim a brand-new vessel before its agency is stamped). `force:true` does NOT bypass; Stage 5.18d transferagency must mutate fields directly and call ReleasePlayerLocks. Boot-time orphan-vessel warning in `LoadExistingAgencies` lists agency-ids referenced by vessels but missing from the registry (corrupted file recovery surface). ServerTest 140→151 (+11), MockClientTest 23→26 (+3). Two Round-1 [MUST FIX] caught (race + comment accuracy); Round-2 caught a build break from a missing using directive — a useful reminder to always rebuild Server.csproj before claiming green.
  - ✅ 5.17c `AgencyScenarioProjector` (session 13, commit `d0bdea38`). Server-side per-player scenario projection. When `PerAgencyCareer=true`, `SendScenarioModules` rewrites the `funds`/`sci`/`rep` root-level scalars in the outgoing ScenarioModule blobs with the requesting client's agency values via precompiled column-0-anchored regex. Read-only — the canonical `CurrentScenarios` is never mutated by projection. Dual-mode silence: gate=off and Sandbox bypass. Boot-time `WarnAboutSavingsLossOnUpgrade` catches the pre-0.31-upgrade scenario where accumulated career savings would silently vanish on first agency mint. `setfunds`/`setscience` admin commands refuse under gate=on with pointer to Stage 5.18d. The Q5 audit hybrid pays off: server-side projection alone covers Funds/Sci/Rep, deleting most of the ~83-site client-Harmony read-path work the original spec assumed. ServerTest 151→163, MockClientTest 26→29.
  - ✅ 5.17d `AgencyContractRouter` (session 13). Q6 hybrid routing — Offered/Generated stay in the shared `ContractSystem` scenario (CC's `ContractPreLoader` undisturbed); Active/Completed/Failed/Cancelled/DeadlineExpired/Withdrawn route per-agency. Persists in `AgencyState.Contracts` via a new CONTRACTS child ConfigNode (Base64-encoded decompressed bytes). Owner-only echo via new `AgencyContractMsgData` (wire enum value 4, channel 22). Three commitments enforced: (a) no Offered persistence per-agency, (b) per-contract exception isolation (try/catch per entry in classification + upsert), (c) `ContractPreLoader` untouched. Inbound `ShareProgressContractsMsgData` is intercepted in `ShareContractsSystem` under gate=on. **Critical Round-1 fixes:** dangling-Offered-slot removal via new `ScenarioDataUpdater.RemoveContractFromSharedOfferedPool` (without this, peer agencies could re-Accept a contract someone else just took); catch-up-on-reconnect via new `AgencySystemSender.SendContractCatchupTo` wired into HandshakeSystem (returning owner receives persisted Contracts before any mutation, so the 5.18a client mirror doesn't land empty); pre-0.31-upgrade `WarnAboutSharedContractsOnUpgrade` boot-time diagnostic flags shared CONTRACTS+CONTRACTS_FINISHED entries that would be inherited by every per-agency player (5.18a's `ContractSystem` projection deferral). **Deferred to 5.18a:** per-agency `ContractSystem` scenario projection (needs an observable client-side consumer); separate Finished-contracts list (PlagueNZ's 1,727-entry bloat pattern). Also fixed a latent `ContractInfo.GetByteCount` under-count caught by the new wire round-trip test. ServerTest 163→177, LmpCommonTest 12→14, MockClientTest 29→36.
  - 5.17 `LockSystem` cross-agency rejection + `AgencyScenarioProjector` (Q5) + `AgencyContractRouter` (Q6) + Share* per-agency routing (spec §12 steps 8-11). 5.17b/5.17d also add the 9 deferred wire messages — see [[reference-agency-wire-extension]] for the recipe (5.17d landed `AgencyContractMsgData`; 8 still deferred).
  - 5.17e/5.17e-1..9 (band 1 closer, session 15) — `AgencySystem.PerAgencyEnabled` combined gate (PerAgencyCareer && Career mode); `AgencyCurrencyRouter` for Funds/Sci/Rep (the actual leak fix); `AgencyTechRouter` + per-agency BUG-025; `AgencyResearchRouter` (ScienceSubject + PartPurchase + ExperimentalPart); `AgencyProgressRouter` (Strategy + Achievement + FacilityUpgrade) + 3 new projector scenario splices; `PerAgencyCareerEnabled` flag on `SettingsReplyMsgData`; doc amendments (destructibles shared-by-design + K1 kerbal grief guard + boot-refusal hardening). Server-side per-agency mode functionally complete; band 2 is client-facing.
  - ✅ 5.18a Client `AgencySystem` mirror (session 16, commit `590136a3`). Four files in new `LmpClient/Systems/Agency/` (Membership pure helper, System, MessageHandler, MessageSender) + NetworkReceiver dispatch + `SettingsServerStructure.PerAgencyCareerEnabled` flag + `ShareContractsMessageHandler.ApplyContractBatch` private→internal hoist. Mirror is deliberately thin: LocalAgency identity + OtherAgencies snapshot. KSP singletons stay authoritative for career state. Three lens reviews (consumer / upgrade / client-harmony) with 4 [SHOULD CONSIDER] applied. LmpClientTest 57→62 (+5 `AgencyMembershipDecisionTest`).
  - ✅ 5.18b Client vessel→agency ownership registry (session 17, commit `674f366b`). **Scope rebound:** original ~35-Harmony-write-site spec was obsoleted by band-1's server-side routers — every Share* event already fires through KSP's stock OnXxxChanged hooks that the existing 11 `Share*Events.cs` files subscribe to. The actually-load-bearing missing piece was the absence of a client-side `vesselId → owningAgencyId` mapping that 5.18c UI labels and 5.18d economy guards need. Three production files + one test: `AgencyMembership.cs` (+`TryParseAgencyId` + `RecordOwnership` preservation rule), `AgencySystem.cs` (+`VesselOwnership` ConcurrentDictionary + `TryGetOwningAgency` + OnDisabled clear with Debug log), `VesselProto.CreateProtoVessel` extracts the field before KSP's ProtoVessel ctor drops it. Consumer-lens caught [MUST FIX] **relay-path overwrite** (original `AddOrUpdate` clobbered real agency ids with Empty on every relay-stripped resend) → fixed with preservation rule (non-Empty writes through; Empty inserts only when absent, never downgrades). Upgrade-lens: no [MUST FIX]; 3 doc-first [SHOULD CONSIDER] applied (reconnect-gap XML, transferagency staleness XML, OnDisabled cleared-count log). LmpClientTest 62→74 (+12).
  - ✅ 5.18c Client per-agency UI (session 18, commit `a35b431b`). Twelve files: new `LmpClient/Windows/Agency/` (AgencyWindow + AgencyDrawer + AgencyCreateWindow + AgencyCreateDrawer — partial-class StatusWindow pattern, window-ids 6726/6727), new pure helper `LmpClient/Systems/Agency/AgencyLabelFormatter.cs` (`FormatVesselAgencyLabel` covering 5.18b's two-state-sentinel rules + `IsDefaultDisplayName` detection of the auto-registered `"{player} Space Agency"` pattern), extension to existing `LmpClient/Systems/Label/LabelEvents.cs` (3 sibling methods append agency suffix), `LmpClient/Windows/Status/StatusDrawer.cs` (Agency toggle gated on PerAgencyCareerEnabled + Career mode), plus `LastCreateReplySuccess`+`LastCreateReplyReason` fields on `AgencySystem` for the rename-dialog banner. **Scope rebound:** spec §6 originally called for new Harmony patches on tracking-station rendering — the session-18 research pass found the existing `LabelEvent.onMapWidgetTextProcessed` + sibling events ALREADY provide the hook (via `LmpClient/Harmony/TrackingStationWidget_Update.cs`), so 3-line LabelEvents extensions suffice. Same pattern as 5.18b's rebound. Two parallel lens reviews (consumer + upgrade per `[[feedback-review-lens-framing]]`) caught 2 [MUST FIX] (stale rejection banner persisted across in-session close+reopen; AgencyCreateWindow.Display missing SPACECENTER scene gate) + 2 [SHOULD FIX] (Status toggle on non-Career was dead button; AgencyWindow zero-scalar render in mid-handshake window looked like data loss → "(syncing…)" placeholder) + 3 [CONSIDER] (TODO XML on RecordOwnership for 5.18d ForceRecordOwnership companion; "Vessels owned by agencies not yet known: N" disclosure for 5.18a snapshot gap; softer non-action default-name hint for pre-0.31 upgraders). LmpClientTest 74→91 (+17 `AgencyLabelFormatterTest` cases pinning every branch). **Deferred to 5.18d (signed off):** `AgencyVisibilityMsgData` wire surface, cross-agency lock-denial toast, force-full-sync-on-reconnect — they all wait on transferagency / economy guards to create the actual downstream demand.
  - ❌ 5.17b SKIPPED — fully obsoleted by Stage 5.17e routers (session 19 scope-check, 2026-05-18). The spec §12 step 11 ask ("each of the 13 Share* systems gets a `PerAgencyCareer` branch applying mutations to the sender's `AgencyState` and echoing only to the sender") is already implemented by the five band-1 routers (AgencyCurrencyRouter / AgencyTechRouter / AgencyResearchRouter / AgencyProgressRouter / AgencyContractRouter). All 11 server-side `Share*System.cs` inbound handlers (Funds, Science, Reputation, Technology, ScienceSubject, PartPurchase, ExperimentalPart, Strategy, Achievement, FacilityUpgrade, Contracts) call the matching `Router.TryRoute*` as the FIRST line of their handler with `if (...) return;` short-circuit before the legacy shared-scenario relay/write — verified directly across all 11 files. Dual-mode silence preserved by router gate-check returning false under `PerAgencyCareer=false` so the legacy path runs unchanged. (Spec's "13" count = 13 ShareProgress*MsgData wire types including 1 base + 1 S→C TechnologyRejected; the 11 C→S types map 1:1 to the 11 systems.) The 7 mutation wire messages 5.17b would have shipped are likewise obsoleted — band-1 routers reuse the existing `AgencyStateMsgData` owner-only echo + persist via `AgencySystem.SaveAgency` instead of per-field delta messages, which is also a cleaner client mirror surface for 5.18a. BUG-025 bracketing concern documented in [AgencyCurrencyRouter.cs:71-82](Server/System/Agency/AgencyCurrencyRouter.cs#L71-L82) as a client-side concern for 5.18a's `AgencyStateMsgData` apply path (already shipped in 5.18a). No tests added/removed by this closure.
  - ✅ 5.18d Admin commands — `listagencies`, `setagency*`, `transferagency`, `deleteagency`, `setvesselagency` (Phase 3 Slice E-2 vessel-level reassignment with MKS-aware per-router migration). Shipped session 38. Recovery-economy ownership enforcement still deferred.
  - ✅ 5.18g Untrusted-cohort hardening (session 38, commit `0d99a81a`) — `AgencyCurrencyRouter` NaN/Infinity guard + snap-back echo; `AgencyContractRouter` per-`ContractGuid` claim map (first-Accept-wins; pre-seed-from-disk; evict-on-deleteagency); StatusDrawer dead-button fix (v1 soak Finding 1).
  - **WOLF Phase 4 (sessions 38-42)** — MKS Wireless Orbital Logistics Function per-agency. All 6 slices shipped on `feature/per-agency`:
    - ✅ Slice A wire shape + AgencyState persistence (`95b65711`)
    - ✅ Slice B server-side depot routing foundation (`f041fd8d`)
    - ✅ Slice B-2 atomic activation (projector splice + 3 player postfixes + hazard predicate) (`b7a3e568`)
    - ✅ Slice B-3 Negotiate-debounce hot path (`0febbd98`)
    - ✅ Slice C Routes router + projector ROUTES splice (FK-swept) (`a9caea6e`)
    - ✅ Slice D Hoppers + Terminals routers + projector splices (`f87ab3ab`)
    - ✅ Slice E CrewRoutes router + cross-agency kerbal authority gate + 5 Harmony postfixes incl. CheckArrived (`d726dc46`)
    - ✅ **Slice F migration helpers — `AgencyWolfMigration.CascadeOnDelete` restores in-flight CrewRoute passengers before `TryDeleteAgency`; `/transferagency` + `/setvesselagency` NO-OP doc blocks; ForkBuildInfo WOLF-R4 expanded to umbrella per MKS-R2 precedent** (this commit)
  - 5.18e/f CC soak + PlagueNZ re-audit + acceptance + merge. After Slice F: v4 cut is the next gate.
- **Stage 6 — Per-agency kerbal roster** — IN PROGRESS on `feature/per-agency` (no separate branch; phases ship as small commits on the same branch as Stage 5). Spec at [docs/research/10-stage6-per-agency-kerbals-spec.md](docs/research/10-stage6-per-agency-kerbals-spec.md) — Phase 6.1 landed amended for 5 pre-implementation gaps (Tourism/Rescue contracts moved to §9 Out of Scope; EVA dual-channel wire flow documented; ProtoCrewMember_Die patch addendum; WriteAtomic for per-agency kerbal writes; external-seat-sync 2018 bug fixed as separable commit).
  - ✅ 6.1 Land spec doc + Q-signoff with pre-implementation gap amendments (`64d5f2e1`)
  - ✅ 6.2 Settings + gate + boot-refusal scaffolding (`c319d475`). `GameplaySettings.PerAgencyKerbalRoster` + `AllowEnablePerAgencyKerbalsOnExistingUniverse` (default false; presets reset both); `AgencySystem.PerAgencyKerbalRosterEnabled` combined gate (composes `PerAgencyEnabled` AND `PerAgencyKerbalRoster`); `WarnAboutSharedKerbalsOnUpgrade` + `RefuseStartupIfKerbalHazardWithoutOverride` with independent override (orthogonal to the existing `AllowEnablePerAgencyOnExistingUniverse` — operator can accept one migration without accepting the other). No handler routing yet. ServerTest 671 → 682 (+11). Dual-mode silence preserved.
  - ✅ 6.3 Per-agency kerbal subdir + stock-4 seeding + delete cascade (`90470f08`). New `AgencySystem.GetKerbalsPathForAgency(Guid)` resolves `Universe/Agencies/{guid:N}/Kerbals/` (sibling of `{guid:N}.txt` — folder + file with same stem coexist on NTFS/ext4/APFS). Private `SeedStockKerbalsForAgency(AgencyState)` writes Jeb/Bill/Bob/Val from `Resources.*_Kerman` templates via `FileHandler.WriteAtomic` with per-file `FileExists` idempotency + lazy `FolderCreate` (all-4-present re-call does zero IO). New `FileHandler.FolderDeleteRecursive` wraps `Directory.Delete(recursive: true)` under the existing per-path lock semaphore. Wired into RegisterAgency mint (after EnsureStartTechSeeded, before SaveAgency), LoadAgencyFromFile backfill (pre-Phase-6.3 agencies + partial-seed recovery), and TryDeleteAgency cascade (Kerbals subdir + empty parent `{guid:N}/` folder inside the existing per-agency lock scope). **Gate choice:** `PerAgencyEnabled` NOT `PerAgencyKerbalRosterEnabled` — disk layout is established up-front for every per-agency-career universe regardless of kerbal-roster setting, so a mid-cohort flip needs no backfill pass (~4kB per agency cost is negligible). Handler routing still untouched; Phase 6.4/6.5 lands it. ServerTest 682 → 693 (+11). Reviewed via server-systems + persistence lenses (0 MUST FIX; 2 SHOULD FIX both forward-looking doc warnings applied — Phase 6.5 race-window guard + sibling-subdir lock-anchor difference).
  - ✅ 6.4 `HandleKerbalsRequest` per-agency filter under combined-gate=on (this commit). New `KerbalSystem.ResolveKerbalsPathForRequester(string playerName)` internal pure helper covers 4 branches: combined gate off → legacy; gate on + `AgencyByPlayerName` miss → legacy + Warning (defensive fallback per spec §3); gate on + mapping + `FolderExists` true → per-agency subdir; gate on + mapping + subdir absent → legacy + Warning. `HandleKerbalsRequest` body wraps the FULL enumerate+ReadFile+ToArray pipeline in a single `IOException` catch (was deferred LINQ — `DirectoryNotFoundException` alone would miss per-file `FileNotFoundException` from a `/deleteagency` cascade interleaving). **Temporary boot-refusal `RefuseStartupIfKerbalWriteRoutingNotYetShipped`** (added in `AgencySystem`) fires unconditionally under `PerAgencyKerbalRoster=true` regardless of universe state or override flags — blocks the half-shipped state where reads route per-agency but writes (still untouched) pool to `Universe/Kerbals/`. The predicate is REMOVED in the Phase 6.5 commit; the two Phase 6.2 tests that previously asserted "boot allowed under override" are temporarily flipped to assert refusal and will flip back when Phase 6.5 ships. Reviews: server-systems lens (1 SHOULD FIX widened catch + 3 CONSIDER) + consumer/upgrade lens (1 MUST FIX boot-refusal added + 1 SHOULD FIX widened catch + 1 MUST FIX spec text corrected — §Q-Migration: legacy kerbals are inaccessible to clients post-opt-in, not "frozen reference set readable to players"). ServerTest 693 → 701 (+7 helper-branch + 1 explicit Phase 6.4 refusal). MockClientTest +2 e2e (two-client distinct-roster with planted unique kerbal proving cross-leak protection + two-client gate-off legacy-shared parity).
  - ✅ 6.5 `HandleKerbalProto` + `HandleKerbalRemove` per-agency routing under combined gate=on (this commit). New internal pure helpers `KerbalSystem.TryWriteKerbalProtoPerAgency(string senderPlayerName, string kerbalName, byte[] data, int numBytes, out Guid senderAgencyId)` + `KerbalSystem.TryDeleteKerbalPerAgency(string senderPlayerName, string kerbalName, out Guid senderAgencyId)` resolve sender→agency, acquire `AgencySystem.GetAgencyLock(senderAgencyId)`, re-check `Agencies.ContainsKey` under the lock (closes the cascade race documented in Phase 6.3 commit `90470f08`), write via the new `FileHandler.WriteAtomic(string, byte[], int)` overload (symmetric with `WriteToFile`'s byte/numBytes shape; includes `ContentChecker.ContentsAreEqual` short-circuit per SS-1) / delete via `FileHandler.FileDelete`, then return a bool routing-outcome. Caller relays to same-agency clients via the new `RelayToSameAgencyClients<T>` inline filter — under the current 1:1 OwningPlayerName design selects zero peers, structure forward-compat with multi-player-per-agency. DROP semantics on resolve failure (NOT legacy fallback — asymmetric with Phase 6.4 read side because writes-to-legacy under gate=on would be silent data loss). K1 scan SKIPPED under gate=on (structurally moot per spec §Q-K1 — Phase 6.4 read filter prevents clients from receiving foreign-agency kerbal names); preserved under gate=off. Phase 6.4 TEMPORARY boot-refusal `RefuseStartupIfKerbalWriteRoutingNotYetShipped` REMOVED in this commit + 2 Phase 6.2 tests reverted from `_UntilPhase65Ships` suffix + 1 explicit-refusal test deleted. New `LogPerAgencyKerbalRosterActiveDiagnostic` boot-time confirmation log under gate=on. **Known limitation deferred to Phase 6.7:** `AgencyWolfMigration.CascadeOnDelete` (Slice F) still writes restored kerbal files to legacy `Universe/Kerbals/` — under gate=on the per-agency read filter never enumerates legacy, so cascade-restored kerbals from `/deleteagency` on an agency with in-flight WOLF CrewRoutes are unreachable to clients of any surviving agency. Phase 6.5 surfaces this via an audible Warning in `CascadeOnDelete` when gate=on + in-flight CrewRoutes present, with manual hand-copy recovery instructions; Phase 6.7 reworks the routing. Reviews: server-systems lens (2 SHOULD FIX + 2 CONSIDER) + consumer/upgrade lens (1 MUST FIX → mitigated via Warning, deferred fix to Phase 6.7 per spec; 2 SHOULD FIX both applied including positive parity test for [[feedback-negative-assertions-lock-in-bugs]]; 3 CONSIDER all addressed). ServerTest 701 → 712 (+8 in `PerAgencyKerbalWriteRoutingTest` + 4 in `FileHandlerAtomicWriteTest` byte-overload − 1 deleted explicit-refusal). MockClientTest +4 e2e (3 routing cases + 1 positive parity for forward-compat same-agency relay).
  - ✅ 6.6 Tracking-station / map / flight UI annotation — `Crew: N (agency B)` label on foreign-agency vessels per spec §2 Q-Render (this commit). Two new pure helpers `AgencyLabelFormatter.IsForeignVessel(vesselId, localAgencyId, vesselOwnership)` + `FormatForeignVesselCrewLabel(crewCount, agencyDisplayName)` follow the established 5.18c testable-helper pattern. New `AgencySystem.ForeignCrewCount` `ConcurrentDictionary<Guid,int>` registry + `TryGetForeignCrewCount` accessor + `OnDisabled` clear (mirrors `VesselOwnership` lifecycle). Populated by `VesselLoader.ScrubInvalidProtoCrew` when `totalRemoved > 0` AND `SettingsSystem.ServerSettings.PerAgencyKerbalRosterEnabled` (combined gate, NOT `PerAgencyCareerEnabled` alone — review-caught MUST FIX); symmetric eviction on `totalRemoved == 0`. New `PerAgencyKerbalRosterEnabled` field on `SettingsReplyMsgData` (tail-bit-read pattern, backward-read-compat — mirrors the existing `PerAgencyCareerEnabled` tail field); server emits `AgencySystem.PerAgencyKerbalRosterEnabled`, client mirrors into `SettingsServerStructure.PerAgencyKerbalRosterEnabled`. Combined gate prevents BUG-023 KerbalProto-after-VesselProto race transients under `PerAgencyCareer=on / PerAgencyKerbalRoster=off` (intermediate Stage 5 → 6 ramp) from seeding misleading "Crew: N (Acme)" labels that imply ownership of what are actually shared kerbals. `LabelEvents.ResolveAgencyDecoration` new private helper wraps the existing 5.18c `ResolveAgencyLabel` + checks `IsForeignVessel` at render time (render-time ownership check is the source of truth — covers the transferagency-to-local race that registry eviction alone can't); render-time miss returns the bare `[agency]` decoration unchanged. All 3 label surfaces (`OnLabelProcessed` / `OnMapLabelProcessed` / `OnMapWidgetTextProcessed`) share the new helper so flight + map + tracking-station rendering stay symmetric. Spec §8 acceptance criterion 4 satisfied: "Agency A's vessel with Jeb aboard renders in Agency B's tracking station — vessel name + crew-count label visible." LmpClientTest 165 → 177 (+12 — 6 cases on `FormatForeignVesselCrewLabel` for zero/negative/single/multi/null-agency/empty-agency; 6 cases on `IsForeignVessel` for null-registry/local-Empty/vessel-absent/owning-Empty-Unassigned/owning-local-False/owning-foreign-True). No KSP in-game runtime test — soak validates the actual rendering per spec §7. Dual-mode silence preserved (gate=off → registry never written, label surfaces unchanged). **Known cosmetic limitation:** non-destructive `UpdateProtoInPlace` / `UnchangedEarlyOut` paths in `VesselLoader` skip the scrub-site population/eviction; a foreign vessel whose crew silently changes between full reloads keeps its prior count until the next destructive reload. Label still renders with the right agency tag; only the count can drift. Documented in `IsForeignVessel` XML; soak per spec §7 validates whether the drift is observable in steady state. Reviews: consumer + upgrade lens (parallel) per [[feedback-review-lens-framing]] — caught 2 [MUST FIX]: (a) upgrade-lens — gate-on-`PerAgencyCareerEnabled`-alone seeded BUG-023-race transients under intermediate config → tightened to combined `PerAgencyKerbalRosterEnabled` (wire-level addition); (b) consumer-lens — pointer-swap / unchanged-early-out paths skip the scrub → documented as limitation per spec §7 soak validation, deferred unless observable. 3 [SHOULD CONSIDER] applied (XML wording fixes for transferagency-staleness claim, "no mid-session eviction" overclaim, Phase 6.7/6.8 forward-looking lifecycle note).
  - 6.7 Re-audit `AgencyWolfCrewRouter` Slice E (read-only confirmed safe by Phase 6.5 multi-lens review — `BuildKerbalAgencyMap` reads vessel-proto bytes only, never touches kerbal files). **Required:** Update Slice F `AgencyWolfMigration.CascadeOnDelete` to route restored kerbal files to the appropriate agency's `Universe/Agencies/{guid:N}/Kerbals/` subdir under gate=on (currently writes legacy `Universe/Kerbals/` regardless of gate — under Phase 6.5 the per-agency read filter never enumerates legacy, so restored kerbals are unreachable; Phase 6.5 surfaces this via an audible Warning at `AgencyWolfMigration.cs` when gate=on + in-flight CrewRoutes are present, but the underlying routing fix is here). Design question: which agency receives the restoration? Under shared-mode the answer was "everyone via the shared roster"; under per-agency mode there's no shared pool. Options: (a) write to the deleted agency's own subdir (about to be cascade-deleted by `TryDeleteAgency` — useless), (b) require operator to specify destination via `/deleteagency alice --restore-to bob`, (c) fall back to legacy as the "loss pool" with operator-recovery instructions. Recommend (b) — explicit operator decision, no silent data movement.
  - 6.8 `/setvesselagency` crew migration: atomic rename per kerbal between source + target agency subdirs. Refuse on same-name collision in target.
  - 6.9 Final Frontier mod-compat audit + per-agency ribbon scenario projection (mirrors S4 DMagic pattern). Optional, only if cohort uses FF.
  - 6.10 Cohort soak + acceptance + merge gate.

---

## Updating This File

- A convention changed? Update it the same session.
- A new system / command / setting landed? Add it under the relevant inventory section.
- A footgun bit you? Note it under "Stack Notes & Patterns Learned" with a date.
- A stage completed or scope shifted? Update "Stage Roadmap."
- AdmiralRadish merged something that affects our turf? Note it under "Stack Notes."

CLAUDE.md is the contract for how this fork is worked on. Stale guidance is worse than no guidance.
